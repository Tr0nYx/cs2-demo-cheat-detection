---
phase: 25-better-result-ui
plan: 03
type: execute
wave: 3
depends_on: ["25-02"]
files_modified:
  - frontend/app/results/[id]/page.tsx
  - frontend/components/ResultsDashboard/ResultDashboardTabs.tsx
  - frontend/__tests__/components/ResultsDashboard/ResultsPageIntegration.test.tsx
autonomous: true
requirements: [PHASE-25-01, PHASE-25-03, PHASE-25-04, PHASE-25-05]
user_setup: []
must_haves:
  truths:
    - "D-01/D-02/D-03: /results/{demoId} renders as a result evidence dashboard with first-viewport orientation"
    - "D-08/D-09/D-10/D-11/D-12: Players, TRACE, Sensitivity, and Viewer modes are separated into focused tabs and reuse existing modules"
    - "D-19/D-20: Route copy uses research/review/confidence language and avoids proof or enforcement language"
    - "D-21: Capped, weak, missing, or parser-limited states remain visible in the assembled page"
  artifacts:
    - path: "frontend/components/ResultsDashboard/ResultDashboardTabs.tsx"
      provides: "Accessible result dashboard tab container for Players, TRACE, Sensitivity, Viewer"
      min_lines: 120
    - path: "frontend/app/results/[id]/page.tsx"
      provides: "Assembled Phase 25 result dashboard route"
      min_lines: 140
    - path: "frontend/__tests__/components/ResultsDashboard/ResultsPageIntegration.test.tsx"
      provides: "Route-level integration tests for tabbed result dashboard"
      min_lines: 140
  key_links:
    - from: "frontend/app/results/[id]/page.tsx"
      to: "frontend/components/DemoDetail/TraceCard.tsx"
      via: "TRACE tab reuses existing component"
      pattern: "TraceCard"
    - from: "frontend/app/results/[id]/page.tsx"
      to: "frontend/components/Analytics/SensitivityTuner.tsx"
      via: "Sensitivity tab reuses existing component"
      pattern: "SensitivityTuner"
    - from: "frontend/app/results/[id]/page.tsx"
      to: "frontend/components/DemoViewer/DemoViewer.tsx"
      via: "Viewer tab reuses existing component"
      pattern: "DemoViewer"
---

<objective>
Assemble the new `/results/{demoId}` evidence dashboard route with focused analysis tabs.

Purpose: Replace the long stacked page with an evidence-first first viewport and separate Players, TRACE, Sensitivity, and Viewer modes while reusing existing data hooks and components.
Output: Updated result route, tab container, and route integration tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-better-result-ui/25-CONTEXT.md
@.planning/phases/25-better-result-ui/25-RESEARCH.md
@.planning/phases/25-better-result-ui/25-01-SUMMARY.md
@.planning/phases/25-better-result-ui/25-02-SUMMARY.md

@frontend/app/results/[id]/page.tsx
@frontend/components/ResultsDashboard/index.ts
@frontend/components/DemoDetail/TraceCard.tsx
@frontend/components/Analytics/SensitivityTuner.tsx
@frontend/components/DemoViewer/DemoViewer.tsx
@frontend/lib/hooks/useDemoFetch.ts
@frontend/lib/hooks/useDemoDetail.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add accessible result dashboard tabs</name>
  <files>frontend/components/ResultsDashboard/ResultDashboardTabs.tsx, frontend/components/ResultsDashboard/index.ts</files>
  <read_first>frontend/components/MatchDetail/MatchReportTabs.tsx, frontend/components/ui/tabs.tsx, frontend/components/ResultsDashboard</read_first>
  <action>
Create `ResultDashboardTabs` using existing tab/segmented-control patterns if present. Tabs must include:
- `Players` as the default mode.
- `TRACE`.
- `Sensitivity`.
- `Viewer`.

Ensure keyboard navigation works through the underlying tab component or explicit button semantics. Keep tab labels compact and avoid explanatory in-app text about how tabs work.
  </action>
  <verify>cd frontend && npx eslint components/ResultsDashboard/ResultDashboardTabs.tsx --max-warnings=0</verify>
  <acceptance_criteria>
    - Players tab is default.
    - Tabs are keyboard reachable and have accessible labels.
    - Tab container supports disabled/unavailable content states without hiding the tab.
  </acceptance_criteria>
  <done>Result dashboard tab shell exists.</done>
</task>

<task type="auto">
  <name>Task 2: Rework `/results/{demoId}` route around dashboard view model</name>
  <files>frontend/app/results/[id]/page.tsx</files>
  <read_first>frontend/app/results/[id]/page.tsx, frontend/lib/result-dashboard.ts, frontend/components/ResultsDashboard/index.ts, frontend/components/Console/index.ts</read_first>
  <action>
Update `frontend/app/results/[id]/page.tsx` so the ready state:
- Builds the `ResultDashboardViewModel` from the fetched demo.
- Shows the existing console header with research notice and useful metadata.
- Places the overview/action area and top review orientation before tabs.
- Uses the Players tab for `PlayerEvidenceTable` plus `PlayerEvidenceDetail`.
- Uses the TRACE tab for existing `TraceCard`.
- Uses the Sensitivity tab for existing `SensitivityTuner` and clearly frames it as exploratory calibration.
- Uses the Viewer tab for existing `DemoViewer`.
- Keeps navigation to history, `/matches/{demoId}`, and valid player profile links.

Preserve not-found, service-unreachable, long-running pending, loading, and unknown-status behavior, but bring copy into the same research-safe console vocabulary where touched.
  </action>
  <verify>cd frontend && npx eslint 'app/results/[id]/page.tsx' --max-warnings=0</verify>
  <acceptance_criteria>
    - Route no longer renders TRACE, Sensitivity, and Viewer as one long always-visible stack.
    - `DemoViewer` is reused, not reimplemented.
    - Existing hooks `useDemoFetch` and `useDemoDetail` remain the data source.
    - Match report link remains available.
  </acceptance_criteria>
  <done>`/results/{demoId}` renders the Phase 25 dashboard structure.</done>
</task>

<task type="auto">
  <name>Task 3: Add result page integration tests</name>
  <files>frontend/__tests__/components/ResultsDashboard/ResultsPageIntegration.test.tsx</files>
  <read_first>frontend/__tests__/components/MatchDetail/MatchPageIntegration.test.tsx, frontend/__tests__/components/ResultsCard.test.tsx, frontend/app/results/[id]/page.tsx</read_first>
  <action>
Add React Testing Library integration tests for the route using mocked hooks/components:
- Loading state renders.
- Done state shows dashboard overview, ranked player table, and Players tab by default.
- Tab switching shows TRACE, Sensitivity, and Viewer panels without rendering all modes as the main page stack.
- Aggregate-only results show aggregate framing and no player profile link.
- Pending/error/no-player states remain usable.
- Links to history, match report, and player profile are present only when valid.
  </action>
  <verify>cd frontend && npm test -- --runTestsByPath __tests__/components/ResultsDashboard/ResultsPageIntegration.test.tsx --watch=false</verify>
  <acceptance_criteria>
    - Tests mock network-dependent hooks and heavy viewer internals.
    - Tests assert default Players mode.
    - Tests assert route-level research-safe labels.
  </acceptance_criteria>
  <done>Route assembly has integration coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Route hooks -> dashboard | Route data may be loading, failed, partial, or stale |
| Tab shell -> heavy modules | Viewer and charts can be expensive; route should avoid unnecessary duplication |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-09 | Denial of Service | ResultDashboardTabs | mitigate | Keep heavy modules behind focused tab panels and avoid duplicating viewer logic |
| T-25-10 | Repudiation | route copy | mitigate | Use research-signal notice and neutral labels in all touched route states |
| T-25-11 | Tampering | sensitivity tab | mitigate | Describe tuner as exploratory calibration and do not imply persisted scores change |
| T-25-12 | Information Disclosure | player links | mitigate | Surface profile links only from validated view-model rows |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `cd frontend && npm test -- --runTestsByPath __tests__/components/ResultsDashboard/ResultsPageIntegration.test.tsx --watch=false`
- [ ] `cd frontend && npx eslint 'app/results/[id]/page.tsx' components/ResultsDashboard --max-warnings=0`
- [ ] `cd frontend && npm run build`
</verification>

<success_criteria>
- `/results/{demoId}` is a tabbed evidence dashboard.
- Players tab is the default and leads with ranked player review rows.
- TRACE, Sensitivity, and Viewer reuse existing modules.
- Route states remain graceful and research-safe.
</success_criteria>

<output>
After completion, create `.planning/phases/25-better-result-ui/25-03-SUMMARY.md` with files changed, tests run, and any route/browser caveats.
</output>
