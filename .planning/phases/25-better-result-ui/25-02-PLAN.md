---
phase: 25-better-result-ui
plan: 02
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - frontend/components/ResultsDashboard/ResultOverviewPanel.tsx
  - frontend/components/ResultsDashboard/PlayerEvidenceTable.tsx
  - frontend/components/ResultsDashboard/PlayerEvidenceDetail.tsx
  - frontend/components/ResultsDashboard/ResultEmptyStates.tsx
  - frontend/components/ResultsDashboard/index.ts
  - frontend/components/FeatureTable.tsx
  - frontend/__tests__/components/ResultsDashboard/ResultDashboardComponents.test.tsx
autonomous: true
requirements: [PHASE-25-01, PHASE-25-02, PHASE-25-04, PHASE-25-05]
user_setup: []
must_haves:
  truths:
    - "D-04/D-05/D-06: Player review starts with a ranked table and uses a selected/expanded detail area instead of always-open player cards"
    - "D-07: Demo-level aggregate rows are visually separated and framed as match-wide research signals"
    - "D-13/D-14/D-15/D-16/D-17: Expanded feature evidence starts with plain-language explanation and keeps raw method/measurements secondary"
    - "D-18/D-19/D-20/D-21: High signals, capped scores, weak evidence, and parser gaps are visible but research-safe"
  artifacts:
    - path: "frontend/components/ResultsDashboard/PlayerEvidenceTable.tsx"
      provides: "Responsive ranked player evidence table with top feature badges and actions"
      min_lines: 160
    - path: "frontend/components/ResultsDashboard/PlayerEvidenceDetail.tsx"
      provides: "Selected-player feature evidence detail with Why this score explanations"
      min_lines: 160
    - path: "frontend/__tests__/components/ResultsDashboard/ResultDashboardComponents.test.tsx"
      provides: "Component coverage for overview, table, detail, aggregate, and empty states"
      min_lines: 150
  key_links:
    - from: "frontend/components/ResultsDashboard/PlayerEvidenceDetail.tsx"
      to: "frontend/lib/result-dashboard.ts"
      via: "renders feature explanation view models"
      pattern: "ResultFeatureExplanation"
---

<objective>
Build reusable result dashboard components for the Players evidence workflow.

Purpose: Replace large always-open player cards with a dense ranked table, selected-player detail, aggregate-only handling, and plain-language feature evidence.
Output: ResultsDashboard component set and component tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-better-result-ui/25-CONTEXT.md
@.planning/phases/25-better-result-ui/25-RESEARCH.md
@.planning/phases/25-better-result-ui/25-01-SUMMARY.md

@frontend/lib/result-dashboard.ts
@frontend/lib/types.ts
@frontend/components/ResultsCard.tsx
@frontend/components/FeatureTable.tsx
@frontend/components/MatchDetail/MatchParticipantTable.tsx
@frontend/components/Console/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create result overview and empty-state panels</name>
  <files>frontend/components/ResultsDashboard/ResultOverviewPanel.tsx, frontend/components/ResultsDashboard/ResultEmptyStates.tsx, frontend/components/ResultsDashboard/index.ts</files>
  <read_first>frontend/components/ResultsCard.tsx, frontend/components/Console/ConsolePanel.tsx, frontend/components/Console/StatusBadge.tsx, frontend/lib/result-dashboard.ts</read_first>
  <action>
Create overview/empty-state components that render:
- Demo status, overall review signal, overall score when available, model/version/provenance when available.
- Download action using existing `downloadDemoUrl`.
- Match report action to `/matches/{demoId}`.
- Pending, error, no-player, aggregate-only, and unknown states with concise research-safe copy.

Use existing console primitives and semantic status badges. Avoid marketing-style hero treatment or oversized explanatory copy.
  </action>
  <verify>cd frontend && npx eslint components/ResultsDashboard/ResultOverviewPanel.tsx components/ResultsDashboard/ResultEmptyStates.tsx --max-warnings=0</verify>
  <acceptance_criteria>
    - Overview displays overall score/verdict without proof/enforcement wording.
    - Empty states distinguish no players from aggregate-only results.
    - Actions include match report and download where appropriate.
  </acceptance_criteria>
  <done>Overview and empty-state components are available for route assembly.</done>
</task>

<task type="auto">
  <name>Task 2: Build ranked player evidence table</name>
  <files>frontend/components/ResultsDashboard/PlayerEvidenceTable.tsx</files>
  <read_first>frontend/components/MatchDetail/MatchParticipantTable.tsx, frontend/lib/result-dashboard.ts, frontend/components/Console/StatusBadge.tsx</read_first>
  <action>
Create `PlayerEvidenceTable` that:
- Accepts ranked player rows from `ResultDashboardViewModel`.
- Shows player name/Steam ID, score band, confidence/evidence state when available, top feature badges, and actions.
- Links to `/players/{steamId}` only when `profileHref` exists.
- Supports a selected row callback and visible selected state.
- Provides a responsive mobile card fallback if the table would not fit.
- Keeps demo aggregate rows out of the real player table.
  </action>
  <verify>cd frontend && npx eslint components/ResultsDashboard/PlayerEvidenceTable.tsx --max-warnings=0</verify>
  <acceptance_criteria>
    - Ranked rows are rendered in the order provided by the view model.
    - Profile links are absent for demo-level placeholders.
    - Mobile layout does not depend on horizontal overflow for core information.
  </acceptance_criteria>
  <done>Players tab can lead with a responsive ranked evidence table.</done>
</task>

<task type="auto">
  <name>Task 3: Build selected player evidence detail and update FeatureTable if reused</name>
  <files>frontend/components/ResultsDashboard/PlayerEvidenceDetail.tsx, frontend/components/FeatureTable.tsx</files>
  <read_first>frontend/components/FeatureTable.tsx, frontend/lib/result-dashboard.ts, frontend/__tests__/components/ResultsCard.test.tsx</read_first>
  <action>
Create `PlayerEvidenceDetail` for the selected real player or aggregate row:
- Lead every feature row/detail with "Why this score?" explanation from the view model/helper.
- Show drivers, limitations, confidence, evidence strength, score caps, warnings, and independent signals.
- Keep raw `method` and raw evidence measurements in a collapsed or visually secondary technical/provenance area.
- Preserve existing `FeatureTable` behavior where still needed, but refactor it only as much as necessary to support explanation-first rendering without breaking existing tests.

If `FeatureTable` is still used elsewhere, keep backwards-compatible props and add optional props rather than forcing all callers to change.
  </action>
  <verify>cd frontend && npm test -- --runTestsByPath __tests__/components/ResultsCard.test.tsx --watch=false</verify>
  <acceptance_criteria>
    - The first expanded detail a reviewer sees is a plain-language explanation.
    - Method names are not the primary evidence text.
    - Existing `ResultsCard` tests either still pass or are intentionally migrated in this phase.
  </acceptance_criteria>
  <done>Selected-player evidence detail is explanation-first and backward-compatible where needed.</done>
</task>

<task type="auto">
  <name>Task 4: Add component coverage</name>
  <files>frontend/__tests__/components/ResultsDashboard/ResultDashboardComponents.test.tsx</files>
  <read_first>frontend/__tests__/components/ResultsCard.test.tsx, frontend/__tests__/components/MatchDetail/MatchReportComponents.test.tsx, frontend/components/ResultsDashboard</read_first>
  <action>
Add component tests covering:
- Overview summary for done, pending, and error states.
- Ranked player table ordering and selected row behavior.
- Real Steam ID player profile links and no link for Steam ID `0`.
- Aggregate-only panel copy.
- Player evidence detail shows `Why this score?`, confidence/cap/warning states, and secondary method/provenance.
- No-player empty state.
  </action>
  <verify>cd frontend && npm test -- --runTestsByPath __tests__/components/ResultsDashboard/ResultDashboardComponents.test.tsx --watch=false</verify>
  <acceptance_criteria>
    - Tests assert research-safe labels such as review signal/evidence/confidence.
    - Tests assert forbidden player attribution is absent for aggregate rows.
    - Tests assert explanation-first evidence is visible.
  </acceptance_criteria>
  <done>Result dashboard components have focused rendering coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| View model -> React components | Components must render supplied states honestly without deriving new scores |
| Player identity -> navigation | Links can imply attribution if placeholders are linked like real profiles |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-05 | Spoofing | PlayerEvidenceTable links | mitigate | Use `profileHref` from view model only; no ad hoc link creation |
| T-25-06 | Repudiation | PlayerEvidenceDetail copy | mitigate | Phrase all signals as research/review evidence and show limitations/caps |
| T-25-07 | Tampering | FeatureTable compatibility | mitigate | Keep score/verdict values read-only from existing Feature data |
| T-25-08 | Denial of Service | responsive table | mitigate | Use stable table/card dimensions and avoid layouts that overflow on mobile |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `cd frontend && npm test -- --runTestsByPath __tests__/components/ResultsDashboard/ResultDashboardComponents.test.tsx __tests__/components/ResultsCard.test.tsx --watch=false`
- [ ] `cd frontend && npx eslint components/ResultsDashboard components/FeatureTable.tsx __tests__/components/ResultsDashboard --max-warnings=0`
- [ ] `cd frontend && npx tsc --noEmit --pretty false`
</verification>

<success_criteria>
- Players evidence UI is table-first and responsive.
- Feature details explain why scores appeared before raw method fields.
- Aggregate-only and unavailable states are explicit.
- Existing scoring/API semantics remain untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/25-better-result-ui/25-02-SUMMARY.md` with files changed, tests run, and any component migration notes.
</output>
