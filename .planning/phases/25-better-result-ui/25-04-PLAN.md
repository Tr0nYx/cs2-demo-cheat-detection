---
phase: 25-better-result-ui
plan: 04
type: execute
wave: 4
depends_on: ["25-03"]
files_modified:
  - frontend/__tests__/components/ResultsDashboard/ResultResearchLanguage.test.tsx
  - frontend/e2e/results-dashboard.spec.ts
  - .planning/phases/25-better-result-ui/25-VERIFICATION.md
autonomous: true
requirements: [PHASE-25-04, PHASE-25-05, PHASE-25-06]
user_setup: []
must_haves:
  truths:
    - "D-18/D-19/D-20/D-21: Research-safe language and visible confidence/limitation states are protected by tests"
    - "Phase 25 final UI is checked at desktop and mobile sizes for first-viewport orientation and no text overlap"
    - "Verification records exact commands and any pre-existing local blockers"
  artifacts:
    - path: "frontend/__tests__/components/ResultsDashboard/ResultResearchLanguage.test.tsx"
      provides: "Forbidden-language and allowed-language regression tests for result dashboard files"
      min_lines: 80
    - path: "frontend/e2e/results-dashboard.spec.ts"
      provides: "Playwright desktop/mobile smoke test for /results/{demoId}"
      min_lines: 100
    - path: ".planning/phases/25-better-result-ui/25-VERIFICATION.md"
      provides: "Phase verification report"
      min_lines: 80
  key_links:
    - from: "frontend/e2e/results-dashboard.spec.ts"
      to: "/results/{demoId}"
      via: "Playwright route smoke"
      pattern: "results"
---

<objective>
Add final language, responsive, and browser verification coverage for the Phase 25 result dashboard.

Purpose: Prove the result UI is research-safe, usable on desktop/mobile, and does not regress critical result states before the phase is marked complete.
Output: Language guard, Playwright smoke spec, and verification report.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-better-result-ui/25-CONTEXT.md
@.planning/phases/25-better-result-ui/25-RESEARCH.md
@.planning/phases/25-better-result-ui/25-03-SUMMARY.md

@frontend/app/results/[id]/page.tsx
@frontend/components/ResultsDashboard
@frontend/e2e/results-polling.spec.ts
@frontend/e2e/match-detail.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add research-language guard</name>
  <files>frontend/__tests__/components/ResultsDashboard/ResultResearchLanguage.test.tsx</files>
  <read_first>frontend/__tests__/components/MatchDetail/MatchResearchLanguage.test.tsx, frontend/app/results/[id]/page.tsx, frontend/components/ResultsDashboard</read_first>
  <action>
Add a Jest test or static source scan for result dashboard UI files that fails on forbidden enforcement/proof terms in rendered copy:
- `cheater`
- `proof` except inside the explicit research disclaimer phrase or the test's forbidden-term list
- `ban`
- `conviction`
- `confirmed cheating`

Also assert allowed language appears in the route/components, such as `research signal`, `review signal`, `confidence`, `evidence`, or `unavailable`.
  </action>
  <verify>cd frontend && npm test -- --runTestsByPath __tests__/components/ResultsDashboard/ResultResearchLanguage.test.tsx --watch=false</verify>
  <acceptance_criteria>
    - New result dashboard files are included in the scan.
    - Test catches forbidden proof/enforcement language.
    - Test confirms research/review/confidence language remains present.
  </acceptance_criteria>
  <done>Result dashboard copy is protected against enforcement-language regressions.</done>
</task>

<task type="auto">
  <name>Task 2: Add Playwright route smoke for desktop and mobile</name>
  <files>frontend/e2e/results-dashboard.spec.ts</files>
  <read_first>frontend/e2e/match-detail.spec.ts, frontend/e2e/results-polling.spec.ts, frontend/app/results/[id]/page.tsx</read_first>
  <action>
Add a Playwright spec for `/results/{demoId}` that uses route mocking if stable local fixture data is not available. Cover:
- Desktop viewport around 1440px.
- Mobile viewport around 390px.
- First viewport shows research notice, status/provenance, overview signal, player evidence table or empty/aggregate state, and tab navigation.
- Players, TRACE, Sensitivity, and Viewer tabs are reachable.
- Text does not visibly overlap in the first viewport and tab controls remain usable.

Mock heavy viewer/tick responses if needed so the smoke test remains stable and focused on route behavior.
  </action>
  <verify>cd frontend && npx playwright test e2e/results-dashboard.spec.ts --project=chromium</verify>
  <acceptance_criteria>
    - Spec targets `/results/`.
    - Spec covers desktop and mobile viewports.
    - Spec verifies tab navigation and first-viewport dashboard orientation.
  </acceptance_criteria>
  <done>Result dashboard has browser smoke coverage.</done>
</task>

<task type="auto">
  <name>Task 3: Run final verification and write report</name>
  <files>.planning/phases/25-better-result-ui/25-VERIFICATION.md</files>
  <read_first>.planning/phases/24-match-detail-page/24-VERIFICATION.md, .planning/phases/25-better-result-ui/25-CONTEXT.md, .planning/phases/25-better-result-ui/*-SUMMARY.md</read_first>
  <action>
Run targeted verification and create `25-VERIFICATION.md` with:
- Exact commands run.
- Pass/fail outcome for unit/component/integration tests.
- Lint/build outcome.
- Playwright smoke outcome.
- Desktop/mobile responsive notes.
- Any known caveats, especially if `npx tsc --noEmit --pretty false` remains blocked by pre-existing e2e type issues outside Phase 25.

The report should verify the phase goal, not merely task completion.
  </action>
  <verify>Test -Path .planning/phases/25-better-result-ui/25-VERIFICATION.md</verify>
  <acceptance_criteria>
    - Verification report includes command outcomes and residual risk.
    - Report explicitly checks research-only language and no scoring semantics changes.
    - Report does not hide blocked checks.
  </acceptance_criteria>
  <done>Phase 25 has a final verification report.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source scan -> user-facing copy | Static tests may scan implementation strings and need explicit allowlists |
| Browser smoke -> route mocks | Mocked API responses must exercise realistic result states without implying backend changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-13 | Repudiation | language guard | mitigate | Fail tests on proof/enforcement terms and assert allowed research language |
| T-25-14 | Denial of Service | Playwright smoke | accept | Keep browser coverage targeted to one route and two viewports |
| T-25-15 | Tampering | route mocks | mitigate | Keep mocks local to tests and verify visible UI behavior only |
| T-25-16 | Repudiation | verification report | mitigate | Record exact commands, failures, caveats, and residual risks |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `cd frontend && npm test -- --runTestsByPath __tests__/lib/result-dashboard.test.ts __tests__/components/ResultsDashboard/ResultDashboardComponents.test.tsx __tests__/components/ResultsDashboard/ResultsPageIntegration.test.tsx __tests__/components/ResultsDashboard/ResultResearchLanguage.test.tsx --watch=false`
- [ ] `cd frontend && npx eslint 'app/results/[id]/page.tsx' components/ResultsDashboard lib/result-dashboard.ts __tests__/components/ResultsDashboard __tests__/lib/result-dashboard.test.ts --max-warnings=0`
- [ ] `cd frontend && npm run build`
- [ ] `cd frontend && npx playwright test e2e/results-dashboard.spec.ts --project=chromium`
</verification>

<success_criteria>
- Research-safe copy is guarded by tests.
- Desktop and mobile smoke checks cover the result dashboard.
- Verification report records exact outcomes and caveats.
- The phase remains UI-only and does not change backend/Python scoring semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/25-better-result-ui/25-04-SUMMARY.md` with verification outcomes, browser smoke status, and any remaining follow-up work.
</output>
