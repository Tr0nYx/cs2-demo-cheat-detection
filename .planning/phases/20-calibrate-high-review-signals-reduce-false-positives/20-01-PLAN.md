---
phase: 20
plan: 01
type: implementation
wave: 1
depends_on: []
files_modified:
  - python/features/base.py
  - python/scoring/weighted_scorer.py
  - tests/test_weighted_scorer.py
autonomous: true
requirements_addressed: ["FEAT-09"]
---

# 20-01-PLAN: Calibration Metadata and Weighted Scorer Guardrails

<objective>
Create the conservative calibration foundation for Phase 20: shared feature confidence/evidence metadata, score-cap semantics, and weighted scorer guardrails that prevent missing or weakly evidenced features from inflating overall high review labels.
</objective>

<context>
Read first:
- `.planning/phases/20-calibrate-high-review-signals-reduce-false-positives/20-CONTEXT.md`
- `.planning/phases/20-calibrate-high-review-signals-reduce-false-positives/20-RESEARCH.md`
- `python/features/base.py`
- `python/scoring/weighted_scorer.py`
- `tests/test_weighted_scorer.py`
</context>

<tasks>
<task>
  <id>20-01-01</id>
  <title>Define calibration metadata contract</title>
  <type>implementation</type>
  <files>
    - python/features/base.py
  </files>
  <action>
    Add a small typed calibration metadata contract or helper conventions around `FeatureResult.metadata` without requiring a database schema change. The contract must support `confidence`, `evidence_strength`, `score_cap_applied`, `score_cap_reason`, `independent_signals`, `sample_count`, and `warnings`. Keep it compatible with existing feature results and JSON persistence.
  </action>
  <acceptance_criteria>
    - Existing feature extractors can still return `FeatureResult` without migration breakage.
    - Metadata shape is documented in code comments or type aliases.
    - No new persisted SQL columns are required.
  </acceptance_criteria>
  <verify>
    $env:PYTHONPATH='python'; python -m py_compile python/features/base.py
  </verify>
</task>

<task>
  <id>20-01-02</id>
  <title>Teach WeightedScorer conservative missing-feature behavior</title>
  <type>implementation</type>
  <files>
    - python/scoring/weighted_scorer.py
    - tests/test_weighted_scorer.py
  </files>
  <action>
    Replace pure proportional redistribution with conservative missing-feature handling. Missing or failed features must not amplify remaining features into `likely_cheating`. Preserve feature aliases added for extractor class names. Add explicit weighting strategy metadata that distinguishes conservative handling from the old proportional behavior.
  </action>
  <acceptance_criteria>
    - One high feature plus missing features does not produce `likely_cheating`.
    - Multiple valid high features can still produce a high overall score.
    - Existing alias support for `AimbotExtractor`, `WallhackExtractor`, `TriggerbotExtractor`, `RecoilExtractor`, `BhopExtractor`, and `SessionConsistencyExtractor` remains covered.
  </acceptance_criteria>
  <verify>
    $env:PYTHONPATH='python'; pytest tests/test_weighted_scorer.py -q
  </verify>
</task>

<task>
  <id>20-01-03</id>
  <title>Add overall high review evidence-family gate</title>
  <type>implementation</type>
  <files>
    - python/scoring/weighted_scorer.py
    - tests/test_weighted_scorer.py
  </files>
  <action>
    Add scorer guardrails so `likely_cheating` normally requires multiple strong feature families. Use feature metadata when present (`evidence_strength=strong`, `confidence=high`) and a conservative fallback when metadata is absent. A single high feature should generally cap the label at `suspicious` unless it is explicitly exceptional and high confidence.
  </action>
  <acceptance_criteria>
    - Tests cover one high feature alone staying below `likely_cheating`.
    - Tests cover two or more strong independent feature families producing `likely_cheating`.
    - Tests cover weak/capped metadata preventing high overall labels.
  </acceptance_criteria>
  <verify>
    $env:PYTHONPATH='python'; pytest tests/test_weighted_scorer.py -q
  </verify>
</task>
</tasks>

<threat_model>
T-20-01: Conservative scoring could silently hide real suspicious behavior.
Mitigation: preserve raw measurements and metadata, cap only effective scores/labels, and keep evidence visible for human review.

T-20-02: Metadata changes could break existing persistence or frontend mapping.
Mitigation: keep fields additive inside existing JSON metadata and preserve current score keys.
</threat_model>

<verification>
- `$env:PYTHONPATH='python'; pytest tests/test_weighted_scorer.py -q`
- `$env:PYTHONPATH='python'; python -m py_compile python/features/base.py python/scoring/weighted_scorer.py`
</verification>

<success_criteria>
- Feature calibration metadata contract exists.
- Missing/failed features no longer inflate overall suspicion.
- Overall high review requires strong evidence from multiple feature families by default.
- Existing scoring aliases remain supported.
</success_criteria>
