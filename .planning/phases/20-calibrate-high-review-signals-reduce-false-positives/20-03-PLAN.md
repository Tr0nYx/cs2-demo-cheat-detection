---
phase: 20
plan: 03
type: implementation
wave: 3
depends_on: ["20-01-PLAN.md", "20-02-PLAN.md"]
files_modified:
  - python/worker.py
  - python/persistence/result_writer.py
  - tests/test_worker_player_slicing.py
  - tests/test_weighted_scorer.py
  - tests/test_phase20_conservative_regression.py
autonomous: true
requirements_addressed: ["WORK-01", "WORK-02", "WORK-03", "WORK-05", "FEAT-09"]
---

# 20-03-PLAN: Regression Harness and Problem Demo Guardrails

<objective>
Add regression coverage that proves the calibrated pipeline does not produce blanket high review signals for weak or uncertain player-specific evidence, and that the known problematic demo path remains player-specific rather than demo-wide.
</objective>

<context>
Read first:
- `.planning/phases/20-calibrate-high-review-signals-reduce-false-positives/20-CONTEXT.md`
- `.planning/phases/20-calibrate-high-review-signals-reduce-false-positives/20-RESEARCH.md`
- `python/worker.py`
- `python/persistence/result_writer.py`
- `tests/test_worker_player_slicing.py`
- `tests/test_weighted_scorer.py`
</context>

<tasks>
<task>
  <id>20-03-01</id>
  <title>Add synthetic conservative regression fixtures</title>
  <type>implementation</type>
  <files>
    - tests/test_phase20_conservative_regression.py
  </files>
  <action>
    Create synthetic `ParsedDemo`/`FeatureResult` fixtures that model normal or weak-proxy behavior across several players. Assert that weak evidence across many players does not generate blanket `likely_cheating` labels, while a separate fixture with multiple strong feature families still can.
  </action>
  <acceptance_criteria>
    - Regression tests cover weak proxy, low sample, missing feature, and multiple strong feature cases.
    - Tests are deterministic and do not require committed `.dem` files.
    - Test names explicitly reference Phase 20 conservative behavior.
  </acceptance_criteria>
  <verify>
    $env:PYTHONPATH='python'; pytest tests/test_phase20_conservative_regression.py -q
  </verify>
</task>

<task>
  <id>20-03-02</id>
  <title>Preserve player-specific slicing and no demo-wide visible result</title>
  <type>test</type>
  <files>
    - tests/test_worker_player_slicing.py
    - python/worker.py
  </files>
  <action>
    Extend player slicing tests so Phase 20 keeps the earlier fix: player actions are attributed to real SteamIDs, opponent footsteps remain available only as context, and `steam_id = 0` demo-wide results are not produced for visible player suspicion.
  </action>
  <acceptance_criteria>
    - Tests assert `steam_id = 0` is excluded from player IDs.
    - Tests assert kill/fire/jump/land attribution remains player-local.
    - Tests assert context events do not become player-owned evidence.
  </acceptance_criteria>
  <verify>
    $env:PYTHONPATH='python'; pytest tests/test_worker_player_slicing.py -q
  </verify>
</task>

<task>
  <id>20-03-03</id>
  <title>Document and optionally run local problem-demo replay check</title>
  <type>verification</type>
  <files>
    - tests/test_phase20_conservative_regression.py
    - .planning/phases/20-calibrate-high-review-signals-reduce-false-positives/20-RESEARCH.md
  </files>
  <action>
    Add a documented local verification path for the known problematic demo ID `019e3a28-60a6-7c96-99c8-34ddd3231268` when the demo exists in local storage. The committed test suite should not depend on the binary demo file, but the plan execution summary should record the local replay result if available.
  </action>
  <acceptance_criteria>
    - CI-safe tests do not require demo binaries.
    - Local replay instructions are clear enough for developer verification.
    - If the demo exists locally, execution records count of results, count of `steam_id = 0`, and number of high review labels after calibration.
  </acceptance_criteria>
  <verify>
    $env:PYTHONPATH='python'; pytest tests/test_phase20_conservative_regression.py tests/test_worker_player_slicing.py tests/test_weighted_scorer.py -q
  </verify>
</task>
</tasks>

<threat_model>
T-20-05: Regression tests could overfit to one problematic demo and miss general false positives.
Mitigation: commit synthetic fixtures for behavior classes and keep real demo replay as optional local verification.

T-20-06: Demo binaries or sensitive user data could be accidentally committed.
Mitigation: do not add `.dem` files; use synthetic fixtures and local-only replay instructions.
</threat_model>

<verification>
- `$env:PYTHONPATH='python'; pytest tests/test_phase20_conservative_regression.py tests/test_worker_player_slicing.py tests/test_weighted_scorer.py -q`
- Optional local replay for `019e3a28-60a6-7c96-99c8-34ddd3231268` if the demo exists in local storage.
</verification>

<success_criteria>
- Conservative scoring behavior is protected by regression tests.
- Player-specific attribution remains protected.
- Known problematic demo has a documented local replay check.
- No demo binaries or generated artifacts are committed.
</success_criteria>
