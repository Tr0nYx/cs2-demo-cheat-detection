---
phase: 20
plan: 04
type: implementation
wave: 4
depends_on: ["20-01-PLAN.md", "20-02-PLAN.md", "20-03-PLAN.md"]
files_modified:
  - frontend/lib/types.ts
  - frontend/lib/api.ts
  - frontend/components/FeatureTable.tsx
  - frontend/components/ResultsCard.tsx
  - frontend/__tests__/components/ResultsCard.test.tsx
autonomous: true
requirements_addressed: ["UI-01"]
---

# 20-04-PLAN: Results UI Confidence and Capped Evidence Display

<objective>
Expose Phase 20 calibration outcomes in the Results UI so users can see which player-specific feature signals are strong, capped, or uncertain, without the frontend inventing scoring behavior.
</objective>

<context>
Read first:
- `.planning/phases/20-calibrate-high-review-signals-reduce-false-positives/20-CONTEXT.md`
- `.planning/phases/20-calibrate-high-review-signals-reduce-false-positives/20-RESEARCH.md`
- `.planning/phases/19-frontend-ui-ux-analysis-console-redesign/19-CONTEXT.md`
- `frontend/lib/types.ts`
- `frontend/lib/api.ts`
- `frontend/components/FeatureTable.tsx`
- `frontend/components/ResultsCard.tsx`
- `frontend/__tests__/components/ResultsCard.test.tsx`
</context>

<tasks>
<task>
  <id>20-04-01</id>
  <title>Map backend confidence and cap metadata into frontend feature model</title>
  <type>implementation</type>
  <files>
    - frontend/lib/types.ts
    - frontend/lib/api.ts
    - frontend/__tests__/components/ResultsCard.test.tsx
  </files>
  <action>
    Extend the frontend `Feature` type and backend mapping to include confidence, evidence strength, score cap status, cap reason, independent signals, and warnings when present in `feature_data` metadata. Preserve compatibility with older results that lack Phase 20 metadata.
  </action>
  <acceptance_criteria>
    - Older backend results still render.
    - Phase 20 metadata is surfaced without changing frontend score calculations.
    - Tests cover capped feature metadata mapping.
  </acceptance_criteria>
  <verify>
    cd frontend; npm test -- ResultsCard.test.tsx --runInBand
  </verify>
</task>

<task>
  <id>20-04-02</id>
  <title>Show capped and uncertain feature states in FeatureTable</title>
  <type>implementation</type>
  <files>
    - frontend/components/FeatureTable.tsx
    - frontend/__tests__/components/ResultsCard.test.tsx
  </files>
  <action>
    Update expanded feature rows to show confidence/evidence strength and score-cap reason when available. Keep copy concise and research-safe. Do not add in-app explanatory walls of text; present the metadata as compact evidence rows.
  </action>
  <acceptance_criteria>
    - Capped features clearly indicate that the signal was limited due to weak/low-confidence evidence.
    - High feature rows can show strong evidence signals when present.
    - UI text remains research-signal wording, not proof.
  </acceptance_criteria>
  <verify>
    cd frontend; npm test -- ResultsCard.test.tsx --runInBand
  </verify>
</task>

<task>
  <id>20-04-03</id>
  <title>Keep overall/player result rendering aligned with backend labels</title>
  <type>implementation</type>
  <files>
    - frontend/components/ResultsCard.tsx
    - frontend/__tests__/components/ResultsCard.test.tsx
  </files>
  <action>
    Ensure overall and per-player status rendering follows backend scores/labels and does not locally escalate weak evidence. Preserve the no-demo-wide-result UX from the earlier player-specific fix.
  </action>
  <acceptance_criteria>
    - Real SteamID player result cards remain visible.
    - Demo-wide aggregate result labels are not reintroduced.
    - Tests cover capped metadata and research-signal copy.
  </acceptance_criteria>
  <verify>
    cd frontend; npm test -- ResultsCard.test.tsx --runInBand
  </verify>
</task>
</tasks>

<threat_model>
T-20-07: Frontend could misrepresent confidence as proof or override backend scoring.
Mitigation: frontend only displays persisted backend metadata and keeps research-signal copy.

T-20-08: New metadata could clutter results and reduce review speed.
Mitigation: show confidence/cap details in expanded rows, not as large always-visible explanations.
</threat_model>

<verification>
- `cd frontend; npm test -- ResultsCard.test.tsx --runInBand`
- `cd frontend; npm run lint`
</verification>

<success_criteria>
- Results UI displays confidence and capped feature information when provided.
- Older results remain compatible.
- Frontend does not invent or escalate scoring behavior.
- Research-only wording remains intact.
</success_criteria>
