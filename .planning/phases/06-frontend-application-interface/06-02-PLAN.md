---
phase: 06-frontend-application-interface
plan: 02
type: execute
wave: 1
depends_on: [06-01]
files_modified:
  - frontend/app/page.tsx
  - frontend/app/results/[id]/page.tsx
  - frontend/app/history/page.tsx
  - frontend/app/error.tsx
  - frontend/app/not-found.tsx
  - frontend/components/UploadForm.tsx
  - frontend/components/ResultsCard.tsx
  - frontend/components/VerdictBadge.tsx
  - frontend/components/FeatureTable.tsx
  - frontend/components/HistoryTable.tsx
  - frontend/components/ErrorBoundary.tsx
  - frontend/lib/hooks/usePolling.ts
  - frontend/lib/hooks/useDemoFetch.ts
  - frontend/lib/hooks/useUploadDemo.ts
  - frontend/__tests__/components/UploadForm.test.tsx
  - frontend/__tests__/components/ResultsCard.test.tsx
  - frontend/__tests__/components/VerdictBadge.test.tsx
  - frontend/__tests__/lib/hooks/usePolling.test.ts
  - frontend/e2e/upload-flow.spec.ts
  - frontend/e2e/results-polling.spec.ts
  - frontend/e2e/history.spec.ts
autonomous: true
requirements: [UI-01]

must_haves:
  truths:
    - "Upload page renders with file input, drag-drop, and submit button"
    - "File validation works: checks type (.dem), size (<100MB), shows errors"
    - "Upload submits to /api/demos endpoint, shows loading state, handles errors"
    - "Results page polls /api/demos/{id} every 2 seconds until complete or 5min timeout"
    - "Results page displays verdict badge (Clean/Suspicious/Likely Cheating) with color and score"
    - "Feature breakdown table shows all 6 features with scores and interpretations"
    - "History page lists all demos, allows filtering by date/verdict, links to results"
    - "Error states show friendly messages: upload failure, API errors, timeout, not found"
    - "All components render without console errors or TypeScript violations"
    - "Unit tests cover components and hooks (Jest + React Testing Library)"
    - "E2E tests cover upload→results→history flows (Playwright)"
    - "Sentry captures upload errors, polling failures, render errors"
  artifacts:
    - path: "frontend/components/UploadForm.tsx"
      provides: "File upload form with drag-drop, validation, submit"
      min_lines: 50
    - path: "frontend/components/ResultsCard.tsx"
      provides: "Results display with polling, verdict, features"
      min_lines: 80
    - path: "frontend/lib/hooks/usePolling.ts"
      provides: "Custom hook for polling /api/demos/{id} with timeout"
      contains: "refetchInterval"
    - path: "frontend/app/results/[id]/page.tsx"
      provides: "Results page with polling hook integration"
      min_lines: 30
    - path: "frontend/__tests__/components/UploadForm.test.tsx"
      provides: "Jest unit tests for upload form"
      min_lines: 40
    - path: "frontend/e2e/upload-flow.spec.ts"
      provides: "Playwright E2E test: upload → results"
      min_lines: 30
  key_links:
    - from: "UploadForm"
      to: "/api/demos (POST)"
      via: "uploadDemo axios call"
      pattern: "axios.post|mutation"
    - from: "ResultsCard"
      to: "/api/demos/{id} (GET)"
      via: "usePolling hook with refetchInterval"
      pattern: "useQuery|refetchInterval"
    - from: "HistoryTable"
      to: "/api/demos (GET)"
      via: "useDemoFetch hook"
      pattern: "useQuery|fetchDemoList"
    - from: "Error states"
      to: "Sentry"
      via: "useEffect + captureException"
      pattern: "captureException|captureMessage"
---

<objective>
Implement the core UI components and pages for demo uploads, analysis results, and history viewing. This plan builds the complete user-facing feature set: upload form with validation and drag-drop, results page with real-time polling, history page with filtering, error handling, and comprehensive testing (Jest + Playwright). Output: fully functional web UI that satisfies requirement UI-01, tested and ready for production use.

Purpose: Deliver the end-user feature set that allows researchers to upload demos, view analysis results with explanations, and track analysis history. All error paths handled gracefully. All components tested.

Output: Complete frontend application with 5 pages (upload, results, history, error, not-found), 7 components, 3 custom hooks, full test coverage, E2E tests for core flows. UI matches design contract from UI-SPEC.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-frontend-application-interface/06-CONTEXT.md
@.planning/phases/06-frontend-application-interface/06-UI-SPEC.md
@.planning/phases/06-frontend-application-interface/06-RESEARCH.md
@.planning/phases/06-frontend-application-interface/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Upload page and UploadForm component</name>
  <files>
    frontend/app/page.tsx
    frontend/components/UploadForm.tsx
    frontend/lib/hooks/useUploadDemo.ts
    frontend/__tests__/components/UploadForm.test.tsx
  </files>
  <action>
    Create UploadForm component with:
    - File input with drag-and-drop (accept .dem files)
    - Display selected file name and size
    - Validation: file type (.dem), file size (<100MB), shows error messages
    - "Upload Demo" button with loading state during submission
    - Error message display on upload failure
    - Optional Steam Match ID text input (per UI-SPEC)
    
    Create useUploadDemo custom hook:
    - Uses axios to POST file to /api/demos (multipart form data)
    - Handles upload progress (optional: show progress bar)
    - Returns loading, error, demo_id states
    - On success, returns demo_id for redirect
    
    Update app/page.tsx (Upload page):
    - Title "Demo Upload & Analysis" (per UI-SPEC)
    - Centered card layout with UploadForm
    - Footer with GitHub link (optional)
    - On successful upload, redirect to /results/{demo_id}
    
    Write Jest tests for UploadForm:
    - File input accepts .dem files
    - Shows error for invalid file type
    - Shows error for file too large
    - Submit button disabled while uploading
    - Success redirects to results page
    
    Reference: UI-SPEC.md Screen 1 (Upload Page), 06-RESEARCH.md File Operations section. Per D-03, error handling is robust with friendly messages.
  </action>
  <verify>
    <automated>
      npm test --prefix ./frontend -- UploadForm.test.tsx --passWithNoTests 2>&1 | grep -q "PASS\|Tests:.*passed" && grep -q "useUploadDemo" ./frontend/lib/hooks/useUploadDemo.ts && grep -q "UploadForm" ./frontend/app/page.tsx && grep -q "Demo Upload" ./frontend/app/page.tsx
    </automated>
  </verify>
  <done>
    UploadForm component renders and validates files. useUploadDemo hook handles file upload to /api/demos. Upload page displays form and handles success/error. Unit tests pass. Component is accessible (labels, error messages announced).
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement Results page with polling and result display components</name>
  <files>
    frontend/app/results/[id]/page.tsx
    frontend/components/ResultsCard.tsx
    frontend/components/VerdictBadge.tsx
    frontend/components/FeatureTable.tsx
    frontend/lib/hooks/usePolling.ts
    frontend/lib/hooks/useDemoFetch.ts
    frontend/__tests__/components/ResultsCard.test.tsx
    frontend/__tests__/lib/hooks/usePolling.test.ts
  </files>
  <action>
    Create usePolling custom hook:
    - Uses React Query useQuery with dynamic refetchInterval
    - Polls /api/demos/{demo_id} every 2 seconds while status === "pending"
    - Stops polling when status === "done" or "error"
    - Maximum timeout: 5 minutes (300 seconds), then shows timeout error
    - Returns: data, loading, error, isTimeout states
    - Implements exponential backoff on network failure (1s, 2s, 4s)
    
    Create VerdictBadge component:
    - Displays verdict (Clean/Suspicious/Likely Cheating) with color-coded background
    - Shows score 0-100 with progress bar
    - Color: Green (0-33), Orange (34-66), Dark Red (67-100) per UI-SPEC
    - Includes icon (checkmark/alert/X) + text + color
    
    Create FeatureTable component:
    - Table with columns: Feature, Score, Interpretation
    - Rows: Aimbot, Triggerbot, Wallhack, Recoil, Bhop, Session Consistency
    - Each score color-coded (Green/Orange/Red)
    - Collapsible "Details" row for technical metadata
    
    Create ResultsCard component:
    - Title: "Analysis Results: {player_name}"
    - Display overall verdict badge (using VerdictBadge)
    - Display per-player section with:
      - Player name + Steam ID
      - Verdict badge
      - Feature breakdown table (using FeatureTable)
    - Loading state: spinner + "Analysis running..."
    - Error state: "Analysis failed" with error message
    
    Create useDemoFetch hook:
    - Wrapper around usePolling that handles all states
    - Returns: results, loading, error, isTimeout
    
    Create app/results/[id]/page.tsx (Results page):
    - Client component ("use client" required for polling/useEffect)
    - Fetch demo with useDemoFetch(params.id)
    - Display ResultsCard with results
    - Show error state if analysis failed
    - Handle timeout: "Analysis taking longer than expected. [Check Status] [Go Back]"
    - Download button for original demo file (GET /api/demos/{id}/download)
    - Breadcrumb: "Upload > Results: {player_name}"
    
    Write Jest tests:
    - VerdictBadge displays correct color/interpretation for scores
    - FeatureTable renders all 6 features
    - ResultsCard handles loading/error/success states
    - usePolling stops polling after 5 minutes
    
    Reference: UI-SPEC.md Screen 2 (Results Page), 06-RESEARCH.md Data Fetching & Polling section. Per D-02, polling contract is 2s interval, 5min timeout. Per D-04, demo download via API endpoint.
  </action>
  <verify>
    <automated>
      npm test --prefix ./frontend -- "ResultsCard|usePolling" --passWithNoTests 2>&1 | grep -q "PASS\|Tests:.*passed" && grep -q "refetchInterval" ./frontend/lib/hooks/usePolling.ts && grep -q "'use client'" ./frontend/app/results/[id]/page.tsx && grep -q "VerdictBadge\|FeatureTable" ./frontend/components/ResultsCard.tsx
    </automated>
  </verify>
  <done>
    Results page renders with polling hook. Verdict badge displays with correct colors. Feature table shows all 6 features. Loading, success, error, and timeout states handled. Download button links to API endpoint. Unit tests pass. Component is accessible (progress announced, error messages clear).
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement History page and HistoryTable component with filtering</name>
  <files>
    frontend/app/history/page.tsx
    frontend/components/HistoryTable.tsx
    frontend/__tests__/components/HistoryTable.test.tsx
  </files>
  <action>
    Create HistoryTable component:
    - Table with columns: Date/Time, Demo ID, Players, Verdict, Score, Actions
    - Each row is a past analysis
    - Verdict badge (Green/Orange/Red) with color
    - Actions: "View" button (links to /results/{id}), "Download", "Delete"
    - Empty state: "No analyses yet. Upload a demo to get started."
    - Loading state: skeleton rows
    - Sortable columns (at minimum: date, verdict)
    - Responsive: on mobile, table becomes card view (one analysis per card)
    
    Create app/history/page.tsx (History page):
    - Server component by default (fetch list once)
    - Fetch /api/demos with pagination (if available)
    - Display HistoryTable with results
    - Optional filters (horizontal bar above table):
      - Date range picker (optional v2 - show all demos for v1)
      - Verdict filter (dropdown: All, Clean, Suspicious, Likely Cheating)
      - Search by demo ID or player name (text input)
    - Handle "View" click: navigate to /results/{id}
    - Handle "Delete" click: show confirmation, DELETE /api/demos/{id}, remove from table
    - Handle "Download" click: trigger download of demo file
    
    Write Jest tests:
    - HistoryTable renders demo list
    - Sort by date works
    - Filter by verdict works
    - Delete confirmation shows, then removes row
    - Empty state displays when no demos
    
    Reference: UI-SPEC.md Screen 3 (History Page), 06-RESEARCH.md Common Pitfalls. Mobile-first responsive design with Tailwind.
  </action>
  <verify>
    <automated>
      npm test --prefix ./frontend -- "HistoryTable" --passWithNoTests 2>&1 | grep -q "PASS\|Tests:.*passed" && grep -q "HistoryTable" ./frontend/app/history/page.tsx && grep -q "demo_id\|verdict" ./frontend/components/HistoryTable.tsx
    </automated>
  </verify>
  <done>
    History page fetches and displays demo list. HistoryTable renders with sort/filter. Empty state, loading state, and delete confirmation all work. Responsive design works on mobile/tablet/desktop. Unit tests pass. Links to results pages work.
  </done>
</task>

<task type="auto">
  <name>Task 4: Implement error handling (error boundaries, error states, retry logic)</name>
  <files>
    frontend/app/error.tsx
    frontend/app/not-found.tsx
    frontend/app/results/[id]/error.tsx
    frontend/components/ErrorBoundary.tsx
    frontend/lib/api.ts (update with retry logic)
  </files>
  <action>
    Create app/error.tsx (global error boundary):
    - Displays "Something went wrong" heading
    - Shows error message (in dev, full stack trace; in prod, generic message)
    - "Try Again" button to reset error boundary
    - Sentry logging: captureException(error) with context
    
    Create app/not-found.tsx:
    - Displays "Page not found"
    - Button: "Go to Upload" linking to home page
    
    Create app/results/[id]/error.tsx (results-specific error):
    - Displays "Analysis failed" or specific error message
    - "Try Again" button to refresh
    - "Go Back" button to history
    
    Create ErrorBoundary component (if needed for additional granularity):
    - Wraps components prone to errors
    - Catches render errors and displays fallback UI
    - Logs to Sentry
    
    Update lib/api.ts with error handling:
    - Axios interceptor for all responses
    - Handle 4xx errors: show user-friendly message
    - Handle 5xx errors: "Server error, try again later"
    - Handle network errors (timeout, connection refused): "Unable to reach server"
    - Retry logic: exponential backoff for network errors (1s, 2s, 4s, max 3 retries)
    - All errors logged to Sentry with context
    
    Per UI-SPEC Screen 4 (Error States):
    - "Upload Failed" dialog with error message and "Try Again" button
    - "Analysis Failed" with "Try Again" or "Go Back"
    - "Not Found" with "Go to Upload"
    
    Reference: 06-RESEARCH.md Error Handling section, Next.js error.tsx and not-found.tsx conventions. Per D-03, robust error handling with graceful degradation.
  </action>
  <verify>
    <automated>
      test -f ./frontend/app/error.tsx && test -f ./frontend/app/not-found.tsx && test -f ./frontend/app/results/[id]/error.tsx && grep -q "captureException\|Sentry" ./frontend/app/error.tsx && grep -q "exponential.*backoff\|retry" ./frontend/lib/api.ts
    </automated>
  </verify>
  <done>
    Error boundaries created for global, page-level, and component-level errors. Error messages are user-friendly and logged to Sentry. Retry logic implemented with exponential backoff. Network failures handled gracefully. All error states display correct UI per UI-SPEC.
  </done>
</task>

<task type="auto">
  <name>Task 5: Write Jest unit tests for components and hooks (70-80% coverage target)</name>
  <files>
    frontend/__tests__/components/UploadForm.test.tsx
    frontend/__tests__/components/ResultsCard.test.tsx
    frontend/__tests__/components/VerdictBadge.test.tsx
    frontend/__tests__/components/HistoryTable.test.tsx
    frontend/__tests__/lib/hooks/usePolling.test.ts
    frontend/__tests__/lib/hooks/useDemoFetch.test.ts
    frontend/__tests__/lib/utils.test.ts
  </files>
  <action>
    Write Jest tests for:
    
    UploadForm.test.tsx:
    - Renders file input, submit button
    - Accepts .dem files, rejects other types
    - Shows error for files >100MB
    - Disables button during upload
    - Calls useUploadDemo on submit
    - Calls onSuccess callback with demo_id
    
    ResultsCard.test.tsx:
    - Renders loading spinner while polling
    - Renders verdict badge and feature table on success
    - Shows error message on API error
    - Shows timeout message after 5 minutes
    - Displays all 6 features in table
    
    VerdictBadge.test.tsx:
    - Green badge for score 0-33
    - Orange badge for score 34-66
    - Red badge for score 67-100
    - Shows correct label (Clean/Suspicious/Likely Cheating)
    - Shows score and progress bar
    
    HistoryTable.test.tsx:
    - Renders demo list
    - Shows empty state when no demos
    - Sort by date ascending/descending
    - Filter by verdict works
    - Delete button shows confirmation
    - Delete removes row on confirm
    
    usePolling.test.ts:
    - Fetches /api/demos/{id} initially
    - Refetches every 2 seconds while pending
    - Stops refetching when done
    - Timeout after 5 minutes
    - Exponential backoff on network error
    
    useDemoFetch.test.ts:
    - Returns loading, success, error states
    - Handles polling timeout
    - Handles API errors
    
    utils.test.ts:
    - formatScore(45) returns "45/100"
    - verdictColor(50) returns orange (#ea580c)
    - verdictLabel(75) returns "Likely Cheating"
    - formatTimestamp() returns formatted date
    
    Target: 70-80% coverage (focus on happy path + key error cases, not exhaustive edge cases)
    
    Reference: 06-RESEARCH.md Testing section, React Testing Library best practices. Per D-05, full unit test suite required.
  </action>
  <verify>
    <automated>
      npm test --prefix ./frontend 2>&1 | grep -q "Tests:.*passed" && npm test --prefix ./frontend -- --coverage 2>&1 | grep -q "Statements.*[7-9][0-9]\|Coverage\s*[7-9][0-9]"
    </automated>
  </verify>
  <done>
    Jest unit tests written for all components and hooks. Tests use React Testing Library best practices (render, screen, user events). Coverage target 70-80% achieved. All tests pass. npm test command runs successfully.
  </done>
</task>

<task type="auto">
  <name>Task 6: Write Playwright E2E tests for core user flows</name>
  <files>
    frontend/e2e/upload-flow.spec.ts
    frontend/e2e/results-polling.spec.ts
    frontend/e2e/history.spec.ts
    frontend/e2e/error-handling.spec.ts
  </files>
  <action>
    Write Playwright E2E tests for:
    
    upload-flow.spec.ts:
    - Navigate to /
    - Select .dem file (or drag-drop)
    - Click "Upload Demo"
    - Wait for redirect to /results/{id}
    - Assert page shows "Analysis Results"
    
    results-polling.spec.ts:
    - Navigate to /results/{id} where demo is still processing
    - Assert "Analysis running..." message appears
    - Wait for up to 30 seconds for analysis to complete
    - Assert verdict badge appears with score
    - Assert feature table displays all 6 features
    - Test timeout scenario (if available in test data)
    
    history.spec.ts:
    - Navigate to /history
    - Assert demo list displays
    - Click on a demo row
    - Assert navigation to /results/{id}
    - Click delete, confirm
    - Assert demo removed from list
    - Test sort by date (ascending/descending)
    - Test filter by verdict
    
    error-handling.spec.ts:
    - Test upload with invalid file (shows error)
    - Test upload with too-large file (shows error)
    - Test network error during polling (shows retry message)
    - Test 404 not found page (navigate to /results/invalid-id)
    
    Target: Cover core happy paths (upload→results) + key error scenarios
    
    Reference: 06-RESEARCH.md Testing section, Playwright official docs. Per D-05, E2E tests run in CI on every commit.
  </action>
  <verify>
    <automated>
      npm run e2e --prefix ./frontend 2>&1 | grep -q "passed\|✓" && test -f ./frontend/e2e/upload-flow.spec.ts && test -f ./frontend/e2e/results-polling.spec.ts && test -f ./frontend/e2e/history.spec.ts
    </automated>
  </verify>
  <done>
    Playwright E2E tests written for core user flows. Tests cover upload, results polling, history, and error handling. npm run e2e command works. Tests ready to run in CI. Baseurl configured to localhost:3000.
  </done>
</task>

<task type="auto">
  <name>Task 7: Verify complete application, test coverage, accessibility, and prepare for deployment</name>
  <files>
    frontend/README.md (update with feature list)
    docker-compose.yml (verify full stack works)
    .planning/phases/06-frontend-application-interface/06-02-SUMMARY.md
  </files>
  <action>
    Comprehensive verification:
    
    1. Build verification:
       - npm run build succeeds in <5s
       - No TypeScript errors or warnings
       - No console errors in dev mode
    
    2. Testing verification:
       - npm test passes all tests
       - Coverage report shows 70-80% coverage
       - npm run e2e passes (or notes test data requirements)
    
    3. Docker verification:
       - docker-compose up starts both Symfony and Next.js
       - curl http://localhost:3000 returns home page
       - curl http://localhost/api/demos returns 200 (from Symfony)
       - Frontend can reach backend via NEXT_PUBLIC_API_URL
    
    4. Accessibility verification:
       - All forms have labels
       - Error messages use aria-describedby
       - Buttons accessible via keyboard (Enter/Space)
       - Color not sole means of conveying information (verdict has text + icon + color)
       - Focus indicators visible
    
    5. Responsive design verification:
       - Mobile (320px): single column layout
       - Tablet (640px): two-column layout
       - Desktop (1024px): full layout with sidebar (if applicable)
       - All interactive elements 44px+ touch target
    
    6. Sentry integration verification:
       - Instrumentation.ts loaded
       - Manual error capture in error boundaries
       - Performance monitoring enabled
    
    7. Update README with:
       - Feature list (upload, results, history, error handling)
       - Screenshots (or placeholders)
       - Testing instructions (npm test, npm run e2e)
       - Deployment steps (docker-compose, build, etc.)
    
    8. Create 06-02-SUMMARY.md with:
       - Task completion summary
       - Test coverage report
       - Known issues (if any)
       - Next steps (future enhancements)
    
    Reference: 06-RESEARCH.md all sections, UI-SPEC.md accessibility section. Per D-05, full testing required. Per D-06, Sentry integration verified.
  </action>
  <verify>
    <automated>
      npm run build --prefix ./frontend 2>&1 | grep -q "Successfully compiled\|✓" && npm test --prefix ./frontend 2>&1 | grep -q "Tests:.*passed" && docker-compose config >/dev/null && grep -q "feature\|component\|page" ./frontend/README.md
    </automated>
  </verify>
  <done>
    Complete application verified: builds, tests pass, Docker Compose runs full stack, accessibility compliant, responsive design works. README updated with features and testing. 06-02-SUMMARY.md created. Ready for production deployment or further feature development.
  </done>
</task>

</tasks>

---

## Success Criteria

**Phase 6 UI-01 Requirement Satisfied:**
- [x] User can upload a .dem file through web UI
- [x] User can view analysis status (polling, loading, complete, error, timeout)
- [x] User can inspect analysis results (verdict, feature scores, explanations)
- [x] User can view analysis history and filter/sort
- [x] User can download original demo file
- [x] All error paths handled gracefully

**Quality Gates:**
- [x] Unit tests (Jest + React Testing Library) at 70-80% coverage
- [x] E2E tests (Playwright) for core user flows in CI
- [x] Error handling (Sentry capture, friendly messages, retry logic)
- [x] Accessibility (WCAG 2.1 AA compliant)
- [x] Responsive design (mobile/tablet/desktop)
- [x] TypeScript strict mode (no `any` types)
- [x] Docker integration (runs in Compose)

**Definition of Done (All Tasks):**
All 7 tasks must complete verification gates before phase completion. After Task 7 verification passes, Phase 6 is feature-complete and ready for:
- Production deployment
- User testing
- Performance optimization (v2)
- Enhanced features (dark mode, WebSockets, analytics — all deferred to v2)

---

## Wave Summary

**Wave 1:** 7 tasks (sequential execution, no parallelization for solo dev)
- Duration: ~4-5 days for solo developer
- Dependency: Requires Plan 01 completion (infrastructure)
- Output: Fully functional frontend application with complete test coverage

**Total Phase Duration:** Plan 01 (2-3 days) + Plan 02 (4-5 days) = 6-8 days for full feature-complete Phase 6

---

*Plan created: 2026-05-15*
