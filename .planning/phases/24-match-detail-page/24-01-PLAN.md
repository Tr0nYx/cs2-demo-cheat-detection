---
phase: 24-match-detail-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/lib/types.ts
  - frontend/lib/match-detail.ts
  - frontend/lib/hooks/useMatchDetail.ts
  - frontend/__tests__/lib/match-detail.test.ts
autonomous: true
requirements: [PHASE-24-01, PHASE-24-02, PHASE-24-03]
user_setup: []
must_haves:
  truths:
    - "Frontend has a normalized match detail view model keyed by demo UUID"
    - "Match detail data composes existing demo detail, round, and event contracts without adding scoring semantics"
    - "Missing team score or team affiliation data is represented as unavailable, not fabricated"
    - "Real Steam IDs can be linked to player profile routes while demo-level placeholder ID 0 is not linked"
  artifacts:
    - path: "frontend/lib/match-detail.ts"
      provides: "Pure match detail normalization helpers"
      min_lines: 120
      exports: ["buildMatchDetailViewModel", "isLinkableSteamId"]
    - path: "frontend/lib/hooks/useMatchDetail.ts"
      provides: "React Query composition hook for match detail page data"
      min_lines: 80
      exports: ["useMatchDetail"]
    - path: "frontend/__tests__/lib/match-detail.test.ts"
      provides: "Unit tests for normalization, missing data, and player links"
      min_lines: 80
  key_links:
    - from: "frontend/lib/hooks/useMatchDetail.ts"
      to: "frontend/lib/hooks/useDemoDetail.ts"
      via: "composes existing detail hook"
      pattern: "useDemoDetail"
    - from: "frontend/lib/hooks/useMatchDetail.ts"
      to: "frontend/lib/hooks/useDemoRounds.ts"
      via: "composes existing rounds hook"
      pattern: "useDemoRounds"
    - from: "frontend/lib/hooks/useMatchDetail.ts"
      to: "frontend/lib/hooks/useDemoEvents.ts"
      via: "composes existing events hook"
      pattern: "useDemoEvents"
---

<objective>
Create the frontend data foundation for the Phase 24 match detail page.

Purpose: Give the page a stable view model that can be planned and tested independently of presentation details.
Output: TypeScript types, pure normalizers, a React hook, and unit tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-match-detail-page/24-CONTEXT.md
@.planning/phases/24-match-detail-page/24-RESEARCH.md

@frontend/lib/types.ts
@frontend/lib/hooks/useDemoDetail.ts
@frontend/lib/hooks/useDemoRounds.ts
@frontend/lib/hooks/useDemoEvents.ts
@frontend/lib/api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add match detail DTO and view-model types</name>
  <files>frontend/lib/types.ts</files>
  <read_first>frontend/lib/types.ts, .planning/phases/24-match-detail-page/24-CONTEXT.md</read_first>
  <action>
Extend `frontend/lib/types.ts` with match-detail page types:
- `MatchParticipantDto` with `steamId`, `name`, `team`, `overallScore`, `overallVerdict`, `features`, and `profileHref`.
- `MatchSummaryDto` with `demoId`, `map`, `outcome`, `status`, `uploadedAt`, `processedAt`, `originalFilename`, `steamMatchId`, `hltvMatchUrl`, `score`, and `scoreUnavailableReason`.
- `MatchDetailViewModel` with `summary`, `participants`, `rounds`, `events`, `flaggedKills`, and `dataAvailability`.

Keep the types frontend-facing and do not add new backend assumptions for score, team, economy, weapons, or duels.
  </action>
  <verify>cd frontend && npx tsc --noEmit --pretty false</verify>
  <acceptance_criteria>
    - `frontend/lib/types.ts` exports `MatchDetailViewModel`.
    - `MatchSummaryDto.score` is nullable and has an unavailable reason field.
    - Participant type includes a nullable `profileHref`.
  </acceptance_criteria>
  <done>Match detail types exist and compile.</done>
</task>

<task type="auto">
  <name>Task 2: Implement pure match detail normalizers</name>
  <files>frontend/lib/match-detail.ts, frontend/__tests__/lib/match-detail.test.ts</files>
  <read_first>frontend/lib/types.ts, frontend/lib/api.ts, frontend/components/ResultsCard.tsx</read_first>
  <action>
Create `frontend/lib/match-detail.ts` with:
- `isLinkableSteamId(steamId: string | null | undefined): boolean`, returning false for empty values and `"0"`.
- `buildMatchSummary(demoDetail, demoStatus)` that maps existing metadata into `MatchSummaryDto`.
- `buildMatchParticipants(demoDetail, demoStatus)` that normalizes analysis result players into participants, preserving scores and features when available.
- `buildFlaggedKills(events)` that returns kill events with non-empty `review_signal.flag_reasons`, sorted by tick.
- `buildMatchDetailViewModel({ demoDetail, demoStatus, rounds, events })`.

Use "Score unavailable from current analysis payload" when no score model exists. Do not infer teams unless the source data explicitly contains a team value.

Add Jest tests for linkable Steam IDs, missing score/team behavior, participant normalization, and flagged kill sorting.
  </action>
  <verify>cd frontend && npm test -- --runTestsByPath __tests__/lib/match-detail.test.ts --watch=false</verify>
  <acceptance_criteria>
    - `frontend/lib/match-detail.ts` exports `buildMatchDetailViewModel` and `isLinkableSteamId`.
    - Tests assert Steam ID `"0"` does not get a profile link.
    - Tests assert score is null when the payload lacks a score.
    - Tests assert flagged kills are sorted by tick.
  </acceptance_criteria>
  <done>Pure view-model construction is covered by unit tests.</done>
</task>

<task type="auto">
  <name>Task 3: Create useMatchDetail hook</name>
  <files>frontend/lib/hooks/useMatchDetail.ts</files>
  <read_first>frontend/lib/hooks/useDemoDetail.ts, frontend/lib/hooks/useDemoFetch.ts, frontend/lib/hooks/useDemoRounds.ts, frontend/lib/hooks/useDemoEvents.ts, frontend/lib/match-detail.ts</read_first>
  <action>
Create `frontend/lib/hooks/useMatchDetail.ts` that:
- Accepts `demoId: string`.
- Calls existing `useDemoFetch(demoId)`, `useDemoDetail(demoId)`, `useDemoRounds(demoId)`, and `useDemoEvents(demoId, { type: 'all' })`.
- Returns `{ data, demo, rounds, events, isLoading, isError, errors, refetch }`.
- Builds `data` with `buildMatchDetailViewModel`.
- Keeps partial data usable when rounds/events fail but demo detail succeeds.
- Does not duplicate API fetch code already owned by the existing hooks.
  </action>
  <verify>cd frontend && npx eslint lib/hooks/useMatchDetail.ts lib/match-detail.ts --max-warnings=0</verify>
  <acceptance_criteria>
    - Hook imports and uses `buildMatchDetailViewModel`.
    - Hook composes existing hooks rather than adding raw fetch calls.
    - Hook exposes partial error information for rounds/events.
  </acceptance_criteria>
  <done>`useMatchDetail` provides the page-ready data contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Symfony API -> Frontend normalizer | Backend data is trusted application data but may be partial or absent for older analyses |
| Viewer event data -> UI | Event payloads can be large and must remain bounded by existing API behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Tampering | match-detail normalizer | mitigate | Treat missing score/team fields as unavailable instead of inferring or fabricating values |
| T-24-02 | Information Disclosure | participant links | mitigate | Link only real Steam IDs; do not create profile links for placeholder ID `0` |
| T-24-03 | Repudiation | research labels | mitigate | Preserve neutral field names and avoid proof/enforcement language in view-model labels |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `cd frontend && npm test -- --runTestsByPath __tests__/lib/match-detail.test.ts --watch=false`
- [ ] `cd frontend && npx eslint lib/hooks/useMatchDetail.ts lib/match-detail.ts --max-warnings=0`
- [ ] `cd frontend && npx tsc --noEmit --pretty false`
</verification>

<success_criteria>
- Match detail view model exists and is tested.
- Hook composes existing demo/detail/round/event hooks.
- Missing score/team data is handled honestly.
- Player profile links are generated only for real Steam IDs.
- No new backend endpoint or scoring behavior is introduced in this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/24-match-detail-page/24-01-SUMMARY.md` with files changed, tests run, and any payload gaps discovered.
</output>
