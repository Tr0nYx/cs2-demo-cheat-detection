---
phase: 24-match-detail-page
plan: 04
type: execute
wave: 3
depends_on: ["24-03"]
files_modified:
  - frontend/__tests__/components/MatchDetail/MatchPageIntegration.test.tsx
  - frontend/__tests__/components/MatchDetail/MatchResearchLanguage.test.tsx
  - frontend/e2e/match-detail.spec.ts
  - .planning/phases/24-match-detail-page/24-VERIFICATION.md
autonomous: true
requirements: [PHASE-24-05, PHASE-24-06]
user_setup: []
must_haves:
  truths:
    - "Match detail route is covered by unit/integration tests"
    - "Research-signal copy guard rejects proof/enforcement language"
    - "Desktop and mobile smoke checks cover the first viewport and viewer section"
    - "Final verification records known local caveats without hiding failures"
  artifacts:
    - path: "frontend/__tests__/components/MatchDetail/MatchPageIntegration.test.tsx"
      provides: "Route-level rendering test for the match page"
      min_lines: 100
    - path: "frontend/__tests__/components/MatchDetail/MatchResearchLanguage.test.tsx"
      provides: "Forbidden-language regression tests"
      min_lines: 60
    - path: "frontend/e2e/match-detail.spec.ts"
      provides: "Playwright smoke coverage for match detail route"
      min_lines: 80
    - path: ".planning/phases/24-match-detail-page/24-VERIFICATION.md"
      provides: "Phase verification report"
      min_lines: 80
  key_links:
    - from: "frontend/e2e/match-detail.spec.ts"
      to: "/matches/{demoId}"
      via: "Playwright route smoke"
      pattern: "matches"
---

<objective>
Add Phase 24 verification coverage and final planning/implementation confidence checks.

Purpose: Prove the match detail page works as a research-safe match report across components, route composition, and browser smoke tests.
Output: Jest tests, Playwright smoke spec, and phase verification report.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-match-detail-page/24-CONTEXT.md
@.planning/phases/24-match-detail-page/24-RESEARCH.md
@.planning/phases/24-match-detail-page/24-03-SUMMARY.md

@frontend/app/matches/[demoId]/page.tsx
@frontend/components/MatchDetail/index.ts
@frontend/__tests__/components/PlayerProfile/PlayerProfileSections.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add match page integration tests</name>
  <files>frontend/__tests__/components/MatchDetail/MatchPageIntegration.test.tsx</files>
  <read_first>frontend/app/matches/[demoId]/page.tsx, frontend/lib/hooks/useMatchDetail.ts, frontend/__tests__/components/PlayerProfile/PlayerProfileSections.test.tsx</read_first>
  <action>
Add Jest/React Testing Library tests that mock `useMatchDetail` and verify:
- Loading state renders without crashing.
- Ready state renders header, participant table, rounds, events, and viewer section shell.
- Partial rounds/events error still renders overview data.
- Player links point to `/players/{steamId}` for linkable participants.
- The page includes a link back to `/results/{demoId}`.
  </action>
  <verify>cd frontend && npm test -- --runTestsByPath __tests__/components/MatchDetail/MatchPageIntegration.test.tsx --watch=false</verify>
  <acceptance_criteria>
    - Integration test mocks all network-dependent hooks.
    - Test includes partial-data state.
    - Test verifies results link and player profile link behavior.
  </acceptance_criteria>
  <done>Match page route behavior is covered by component integration tests.</done>
</task>

<task type="auto">
  <name>Task 2: Add research language regression tests</name>
  <files>frontend/__tests__/components/MatchDetail/MatchResearchLanguage.test.tsx</files>
  <read_first>frontend/components/MatchDetail, frontend/app/matches/[demoId]/page.tsx, frontend/lib/research-context.ts</read_first>
  <action>
Add tests or a static source scan that fails if Phase 24 match-detail files contain forbidden terms in rendered copy:
- `cheater`
- `proof`
- `ban`
- `conviction`
- `confirmed cheating`

The test may allow source comments only if they explicitly appear inside the forbidden-term test itself. Prefer asserting allowed language such as "research signal", "review signal", and "not proof" appears where expected.
  </action>
  <verify>cd frontend && npm test -- --runTestsByPath __tests__/components/MatchDetail/MatchResearchLanguage.test.tsx --watch=false</verify>
  <acceptance_criteria>
    - Test fails on forbidden enforcement/proof language in match-detail UI files.
    - Test confirms "Research signals only, not proof" or equivalent disclaimer appears.
  </acceptance_criteria>
  <done>Research-safe copy is protected by regression coverage.</done>
</task>

<task type="auto">
  <name>Task 3: Add Playwright smoke and verification report</name>
  <files>frontend/e2e/match-detail.spec.ts, .planning/phases/24-match-detail-page/24-VERIFICATION.md</files>
  <read_first>frontend/e2e, frontend/app/matches/[demoId]/page.tsx, .planning/phases/23-player-profile-detail/23-VERIFICATION.md</read_first>
  <action>
Add a Playwright smoke spec for `/matches/{demoId}` that:
- Uses route mocking if stable local demo data is not guaranteed.
- Checks desktop viewport around 1440px and mobile viewport around 390px.
- Verifies the first viewport shows match summary, research disclaimer, participants or empty state, and section navigation.
- Verifies the viewer/heatmap section is reachable without overlapping text.

Create `24-VERIFICATION.md` summarizing:
- Commands run.
- Passing/failing status.
- Browser smoke result.
- Any local caveats, such as unavailable database fixtures or dev-server assumptions.
  </action>
  <verify>cd frontend && npx playwright test e2e/match-detail.spec.ts --project=chromium</verify>
  <acceptance_criteria>
    - Playwright spec targets `/matches/`.
    - Spec covers desktop and mobile viewports.
    - Verification report records exact commands and outcomes.
  </acceptance_criteria>
  <done>Phase 24 has browser smoke coverage and a final verification report.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test mocks -> page route | Tests must not hide runtime API assumptions |
| Browser smoke -> local dev server | Playwright depends on a running app and stable mocked or fixture data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-10 | Repudiation | verification report | mitigate | Record exact commands and any blocked checks |
| T-24-11 | Tampering | route mocks | mitigate | Keep mocks local to Playwright tests and verify route-visible behavior only |
| T-24-12 | Denial of Service | Playwright smoke | accept | Use one targeted spec with two viewports, not a broad test suite |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `cd frontend && npm test -- --runTestsByPath __tests__/components/MatchDetail/MatchPageIntegration.test.tsx __tests__/components/MatchDetail/MatchResearchLanguage.test.tsx --watch=false`
- [ ] `cd frontend && npx eslint app/matches/[demoId]/page.tsx components/MatchDetail __tests__/components/MatchDetail --max-warnings=0`
- [ ] `cd frontend && npx tsc --noEmit --pretty false`
- [ ] `cd frontend && npm run build`
- [ ] `cd frontend && npx playwright test e2e/match-detail.spec.ts --project=chromium`
</verification>

<success_criteria>
- Tests cover page rendering, partial data, links, and language safety.
- Playwright smoke covers desktop and mobile route behavior.
- Typecheck/build remain clean or documented if blocked by pre-existing unrelated issues.
- Verification report exists with exact outcomes.
- No live/enforcement functionality is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/24-match-detail-page/24-04-SUMMARY.md` with tests run, browser smoke results, and verification caveats.
</output>
