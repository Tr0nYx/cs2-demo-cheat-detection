---
phase: 24-match-detail-page
plan: 03
type: execute
wave: 2
depends_on: ["24-01", "24-02"]
files_modified:
  - frontend/app/matches/[demoId]/page.tsx
  - frontend/components/MatchDetail/MatchRoundsSection.tsx
  - frontend/components/MatchDetail/MatchEventsSection.tsx
  - frontend/components/MatchDetail/MatchViewerSection.tsx
  - frontend/components/MatchDetail/index.ts
  - frontend/app/results/[id]/page.tsx
  - frontend/components/DemoHistoryTable.tsx
autonomous: true
requirements: [PHASE-24-01, PHASE-24-02, PHASE-24-03, PHASE-24-04, PHASE-24-05]
user_setup: []
must_haves:
  truths:
    - "User can open `/matches/{demoId}` for a match-level report"
    - "Match page shows summary, participants, rounds, events, and viewer/heatmap access"
    - "Existing `/results/{demoId}` remains available and links to the match page"
    - "Round and event sections use existing viewer API data and do not duplicate scoring logic"
    - "The page displays the research disclaimer above the match report"
  artifacts:
    - path: "frontend/app/matches/[demoId]/page.tsx"
      provides: "Canonical match detail route"
      min_lines: 160
    - path: "frontend/components/MatchDetail/MatchRoundsSection.tsx"
      provides: "Round list with winner, reason, duration, kills, first kill, and bomb plant fields"
      min_lines: 90
    - path: "frontend/components/MatchDetail/MatchEventsSection.tsx"
      provides: "Flagged kill and grenade/damage event review section"
      min_lines: 100
    - path: "frontend/components/MatchDetail/MatchViewerSection.tsx"
      provides: "DemoViewer/heatmap entry section"
      min_lines: 70
  key_links:
    - from: "frontend/app/matches/[demoId]/page.tsx"
      to: "frontend/lib/hooks/useMatchDetail.ts"
      via: "uses match detail hook"
      pattern: "useMatchDetail"
    - from: "frontend/components/MatchDetail/MatchViewerSection.tsx"
      to: "frontend/components/DemoViewer/DemoViewer.tsx"
      via: "embeds existing viewer"
      pattern: "DemoViewer"
    - from: "frontend/app/results/[id]/page.tsx"
      to: "/matches/{demoId}"
      via: "adds match report navigation"
      pattern: "matches"
---

<objective>
Assemble the canonical match detail route and connect it to existing result/history navigation.

Purpose: Deliver the actual user-facing Phase 24 experience using the data foundation and components from Plans 01 and 02.
Output: `/matches/{demoId}` route, rounds/events/viewer sections, and links from existing surfaces.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-match-detail-page/24-CONTEXT.md
@.planning/phases/24-match-detail-page/24-RESEARCH.md
@.planning/phases/24-match-detail-page/24-01-SUMMARY.md
@.planning/phases/24-match-detail-page/24-02-SUMMARY.md

@frontend/app/results/[id]/page.tsx
@frontend/components/DemoHistoryTable.tsx
@frontend/components/DemoViewer/DemoViewer.tsx
@frontend/components/DemoViewer/SuspicionPanel.tsx
@frontend/lib/hooks/useMatchDetail.ts
@frontend/components/MatchDetail/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build route shell and overview section</name>
  <files>frontend/app/matches/[demoId]/page.tsx</files>
  <read_first>frontend/app/results/[id]/page.tsx, frontend/app/players/[playerId]/page.tsx, frontend/components/ResearchDisclaimerBanner.tsx, frontend/lib/hooks/useMatchDetail.ts, frontend/components/MatchDetail/index.ts</read_first>
  <action>
Create `frontend/app/matches/[demoId]/page.tsx` as a client page that:
- Reads `demoId` from `useParams()`.
- Uses `useMatchDetail(demoId)`.
- Renders `ConsolePage width="wide"`, `ResearchDisclaimerBanner`, `MatchHeader`, `MatchSectionTabs`, and `MatchParticipantTable`.
- Includes overview anchors with IDs `overview`, `rounds`, `events`, and `viewer`.
- Handles not-found, service-error, loading, pending-analysis, and partial-data states.
- Provides action links back to `/results/{demoId}` and to download demo when possible.

Keep `/results/{demoId}` untouched as the analysis-specific page.
  </action>
  <verify>cd frontend && npx eslint app/matches/[demoId]/page.tsx --max-warnings=0</verify>
  <acceptance_criteria>
    - Route file exists at `frontend/app/matches/[demoId]/page.tsx`.
    - Page renders `ResearchDisclaimerBanner`.
    - Page links to `/results/${demoId}`.
    - Page uses `useParams()` and `useMatchDetail()`.
  </acceptance_criteria>
  <done>Match route shell and overview are functional.</done>
</task>

<task type="auto">
  <name>Task 2: Add rounds, events, and viewer sections</name>
  <files>frontend/components/MatchDetail/MatchRoundsSection.tsx, frontend/components/MatchDetail/MatchEventsSection.tsx, frontend/components/MatchDetail/MatchViewerSection.tsx, frontend/components/MatchDetail/index.ts, frontend/app/matches/[demoId]/page.tsx</files>
  <read_first>frontend/components/DemoViewer/DemoViewer.tsx, frontend/components/DemoViewer/SuspicionPanel.tsx, frontend/lib/types.ts, frontend/components/MatchDetail/MatchEmptyState.tsx</read_first>
  <action>
Create:
- `MatchRoundsSection` that renders round number, winner, end reason, duration, kills, first kill tick, and bomb plant status from `DemoRoundDto[]`.
- `MatchEventsSection` that renders flagged kills first, then compact kill/grenade/damage summaries when available. Use "review signal" labels and seek tick metadata, but do not implement new playback state here.
- `MatchViewerSection` that embeds existing `DemoViewer` with `demoId`, `mapName`, and `analyzed` status. It should introduce the section as a review workspace, not as proof.

Wire all three sections into `frontend/app/matches/[demoId]/page.tsx`.
  </action>
  <verify>cd frontend && npx eslint components/MatchDetail/MatchRoundsSection.tsx components/MatchDetail/MatchEventsSection.tsx components/MatchDetail/MatchViewerSection.tsx app/matches/[demoId]/page.tsx --max-warnings=0</verify>
  <acceptance_criteria>
    - Rounds section handles an empty rounds array with `MatchEmptyState`.
    - Events section highlights flagged kills using `review_signal.flag_reasons`.
    - Viewer section imports and renders `DemoViewer`.
    - No component adds new scoring or detection labels.
  </acceptance_criteria>
  <done>Rounds, events, and viewer sections are integrated into the match page.</done>
</task>

<task type="auto">
  <name>Task 3: Link existing surfaces to match detail</name>
  <files>frontend/app/results/[id]/page.tsx, frontend/components/DemoHistoryTable.tsx</files>
  <read_first>frontend/app/results/[id]/page.tsx, frontend/components/DemoHistoryTable.tsx, frontend/components/Console/ConsoleHeader.tsx</read_first>
  <action>
Add match-detail navigation without breaking existing flows:
- On `/results/{demoId}`, add a secondary action/link labeled "Match report" or "Open match report" pointing to `/matches/{demoId}`.
- In `DemoHistoryTable`, keep the row click behavior leading to `/results/{demoId}` unless changing it would be clearly more ergonomic; add an accessible secondary link/action to `/matches/{demoId}` for analyzed demos.
- Ensure pending/error demos do not imply viewer data is ready.
  </action>
  <verify>cd frontend && npx eslint app/results/[id]/page.tsx components/DemoHistoryTable.tsx --max-warnings=0</verify>
  <acceptance_criteria>
    - Results page contains a link to `/matches/${demoId}`.
    - History table exposes match-report navigation for done demos.
    - Existing row navigation to results still works.
  </acceptance_criteria>
  <done>Users can reach match detail from existing result/history surfaces.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Route params -> API calls | Demo ID comes from URL and existing hooks/API validation handle invalid IDs |
| Existing viewer API -> embedded viewer | Viewer data remains bounded by existing endpoints and cache generation |
| Results/history -> match navigation | Links must not imply analysis is complete when demo status is pending/error |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-07 | Spoofing | route param | mitigate | Use existing demo APIs and not client-only trust for demo status |
| T-24-08 | Denial of Service | viewer section | mitigate | Reuse existing `DemoViewer` and its bounded tick/heatmap requests |
| T-24-09 | Repudiation | navigation copy | mitigate | Use "match report" and "review workspace" labels, never proof/enforcement terms |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `cd frontend && npx eslint app/matches/[demoId]/page.tsx components/MatchDetail app/results/[id]/page.tsx components/DemoHistoryTable.tsx --max-warnings=0`
- [ ] `cd frontend && npx tsc --noEmit --pretty false`
- [ ] Manually inspect rendered route with a known demo ID if local data is available.
</verification>

<success_criteria>
- `/matches/{demoId}` route renders the full match detail page.
- Existing `/results/{demoId}` remains available.
- Results/history provide clear navigation to the match page.
- Rounds/events/viewer use existing APIs and components.
- Research disclaimer and neutral language are present on the route.
</success_criteria>

<output>
After completion, create `.planning/phases/24-match-detail-page/24-03-SUMMARY.md` with files changed, navigation decisions, and verification results.
</output>
