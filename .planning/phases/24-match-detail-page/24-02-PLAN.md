---
phase: 24-match-detail-page
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/components/MatchDetail/MatchHeader.tsx
  - frontend/components/MatchDetail/MatchParticipantTable.tsx
  - frontend/components/MatchDetail/MatchSectionTabs.tsx
  - frontend/components/MatchDetail/MatchEmptyState.tsx
  - frontend/components/MatchDetail/index.ts
  - frontend/__tests__/components/MatchDetail/MatchReportComponents.test.tsx
autonomous: true
requirements: [PHASE-24-01, PHASE-24-02, PHASE-24-05]
user_setup: []
must_haves:
  truths:
    - "Match report components render a csstats-inspired but project-branded match summary"
    - "Components use console UI primitives and research-safe labels"
    - "Participant rows link to player profiles only when a profile href is supplied"
    - "Score, team, and event gaps have explicit empty states"
  artifacts:
    - path: "frontend/components/MatchDetail/MatchHeader.tsx"
      provides: "Match summary header with map, status, provenance, and unavailable score state"
      min_lines: 80
    - path: "frontend/components/MatchDetail/MatchParticipantTable.tsx"
      provides: "Participant table with research-signal score columns and player links"
      min_lines: 100
    - path: "frontend/components/MatchDetail/MatchSectionTabs.tsx"
      provides: "Accessible section navigation for overview, rounds, events, and viewer"
      min_lines: 60
    - path: "frontend/__tests__/components/MatchDetail/MatchReportComponents.test.tsx"
      provides: "Component tests for rendering, links, and forbidden language"
      min_lines: 100
  key_links:
    - from: "frontend/components/MatchDetail/MatchParticipantTable.tsx"
      to: "/players/{playerId}"
      via: "profileHref prop rendered with Next Link"
      pattern: "profileHref"
---

<objective>
Create reusable match-report components for the Phase 24 page.

Purpose: Separate match detail presentation from route assembly so the route can stay small and the components can be tested directly.
Output: Header, participant table, section tabs, empty state components, exports, and component tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-match-detail-page/24-CONTEXT.md
@.planning/phases/24-match-detail-page/24-RESEARCH.md

@frontend/components/Console/index.ts
@frontend/components/Console/ConsolePanel.tsx
@frontend/components/Console/StatusBadge.tsx
@frontend/components/ResearchDisclaimerBanner.tsx
@frontend/components/ResultsCard.tsx
@frontend/components/PlayerProfile/DemoHistorySection.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create match summary header</name>
  <files>frontend/components/MatchDetail/MatchHeader.tsx, frontend/components/MatchDetail/MatchEmptyState.tsx</files>
  <read_first>frontend/components/Console/ConsoleHeader.tsx, frontend/components/Console/StatusBadge.tsx, frontend/components/ResearchDisclaimerBanner.tsx</read_first>
  <action>
Create `MatchHeader` that accepts `summary: MatchSummaryDto` and renders:
- Map and match/demo title.
- Analysis status badge.
- Outcome badge if available.
- Demo ID, upload time, processed time, original filename, Steam match ID, and HLTV URL when present.
- Score area showing score when available, otherwise "Score unavailable" with the reason from the view model.
- Action slots for result/download/viewer links supplied by the route.

Create `MatchEmptyState` for reusable empty states with `title`, `description`, and optional action.

Use console primitives and compact data-dense layout. Do not use a marketing hero, decorative background, or in-app explanatory feature copy.
  </action>
  <verify>cd frontend && npx eslint components/MatchDetail/MatchHeader.tsx components/MatchDetail/MatchEmptyState.tsx --max-warnings=0</verify>
  <acceptance_criteria>
    - Header renders "Score unavailable" when `summary.score` is null.
    - Header renders status and provenance fields without assuming all metadata exists.
    - Empty state component is reusable and does not mention proof/cheaters/bans.
  </acceptance_criteria>
  <done>Match summary header and empty state components exist.</done>
</task>

<task type="auto">
  <name>Task 2: Create participant table and section tabs</name>
  <files>frontend/components/MatchDetail/MatchParticipantTable.tsx, frontend/components/MatchDetail/MatchSectionTabs.tsx, frontend/components/MatchDetail/index.ts</files>
  <read_first>frontend/components/Console/StatusBadge.tsx, frontend/components/DemoHistoryTable.tsx, frontend/components/PlayerProfile/ProfileNav.tsx</read_first>
  <action>
Create `MatchParticipantTable` that:
- Accepts `participants: MatchParticipantDto[]`.
- Shows player name, Steam ID, team if available, overall review signal, verdict, and top feature signals.
- Uses "review signal" and "research signal" language.
- Renders `Link` to `profileHref` only when non-null.
- Handles zero participants with `MatchEmptyState`.
- Uses a responsive table on desktop and stacked rows on mobile.

Create `MatchSectionTabs` with stable section IDs: `overview`, `rounds`, `events`, `viewer`.
It should be keyboard accessible, use buttons or anchor links, and not resize the layout when active state changes.

Export all MatchDetail components through `frontend/components/MatchDetail/index.ts`.
  </action>
  <verify>cd frontend && npx eslint components/MatchDetail/MatchParticipantTable.tsx components/MatchDetail/MatchSectionTabs.tsx components/MatchDetail/index.ts --max-warnings=0</verify>
  <acceptance_criteria>
    - Participant table contains the text "review signal" for score columns.
    - Rows without `profileHref` render plain text, not a broken link.
    - Tabs expose the four required section IDs.
  </acceptance_criteria>
  <done>Participant table and tabs are ready for route assembly.</done>
</task>

<task type="auto">
  <name>Task 3: Add component tests and copy guard</name>
  <files>frontend/__tests__/components/MatchDetail/MatchReportComponents.test.tsx</files>
  <read_first>frontend/__tests__/components/PlayerProfile/PlayerProfileSections.test.tsx, frontend/components/MatchDetail/MatchHeader.tsx, frontend/components/MatchDetail/MatchParticipantTable.tsx</read_first>
  <action>
Add component tests that verify:
- Header renders metadata and unavailable score state.
- Participant table links only rows with `profileHref`.
- Empty participant list shows a useful empty state.
- Section tabs render all four section labels.
- Rendered text does not include forbidden enforcement/proof terms: "cheater", "proof", "ban", "conviction".
  </action>
  <verify>cd frontend && npm test -- --runTestsByPath __tests__/components/MatchDetail/MatchReportComponents.test.tsx --watch=false</verify>
  <acceptance_criteria>
    - Tests include forbidden-language assertions.
    - Tests cover null score and null profile link behavior.
    - Tests pass without needing network calls.
  </acceptance_criteria>
  <done>Match report components are tested in isolation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Match view model -> rendered UI | Display code must not overstate partial data or scores |
| Player ID -> profile link | Only valid persisted player IDs should create internal navigation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-04 | Spoofing | participant table | mitigate | Render provided player IDs plainly and only link when `profileHref` is supplied by normalizer |
| T-24-05 | Repudiation | match header | mitigate | Show provenance fields and unavailable states instead of hiding missing payload data |
| T-24-06 | Information Disclosure | metadata display | accept | Display only fields already returned by authenticated application APIs |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `cd frontend && npm test -- --runTestsByPath __tests__/components/MatchDetail/MatchReportComponents.test.tsx --watch=false`
- [ ] `cd frontend && npx eslint components/MatchDetail __tests__/components/MatchDetail/MatchReportComponents.test.tsx --max-warnings=0`
- [ ] Scan rendered strings for forbidden enforcement language.
</verification>

<success_criteria>
- Match report presentation components exist and are exported.
- Components are responsive, compact, and console-style.
- Missing score/team data is visible as an empty/unavailable state.
- Player links are safe and conditional.
- Research-safe copy is enforced by tests.
</success_criteria>

<output>
After completion, create `.planning/phases/24-match-detail-page/24-02-SUMMARY.md` with files changed, tests run, and any UI tradeoffs.
</output>
