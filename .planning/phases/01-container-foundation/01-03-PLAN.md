---
phase: 01-container-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: ["docker/python/Dockerfile", "python/requirements.txt", "python/worker.py", "python/.gitkeep", "data/demo-storage/.gitkeep", "data/recoil_patterns/.gitkeep"]
autonomous: true
requirements: ["INFR-01", "INFR-03", "INFR-05"]
user_setup: []
must_haves:
  truths:
    - "INFR-01: Python service has a concrete Docker runtime in the local Compose stack."
    - "INFR-03: Python runtime switches to a non-root application user."
    - "INFR-05: Demo storage path exists and is compatible with worker access."
    - "D-05/D-06: Python permissions are pragmatic for bind mounts and storage volumes."
    - "D-07/D-08: Python dependency and worker defaults align with the full .env.example contract."
  artifacts:
    - path: "docker/python/Dockerfile"
      provides: "Python 3.12 worker runtime with non-root user"
      contains: "USER app"
    - path: "python/requirements.txt"
      provides: "Pinned Python dependency baseline"
      contains: "demoparser2"
    - path: "python/worker.py"
      provides: "Minimal worker entrypoint placeholder for container startup"
      contains: "PYTHON_WORKER_QUEUE"
    - path: "data/demo-storage/.gitkeep"
      provides: "Tracked demo storage directory placeholder"
  key_links:
    - from: "docker/python/Dockerfile"
      to: "python/requirements.txt"
      via: "pip install requirements"
      pattern: "requirements.txt"
    - from: "python/worker.py"
      to: ".env.example"
      via: "PYTHON_WORKER_QUEUE environment variable"
      pattern: "PYTHON_WORKER_QUEUE"
    - from: "docker-compose.yml"
      to: "data/demo-storage"
      via: "demo_storage volume mounted at worker storage path"
      pattern: "demo_storage"
---

<objective>
Create the Python worker runtime and storage-permission foundation.

Purpose: Make the analysis-worker side of the Compose stack buildable and aligned with future parser, feature extraction, and ML work.
Output: Python Dockerfile, dependency baseline, minimal worker entrypoint, and storage directory conventions.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-container-foundation/01-CONTEXT.md
@.planning/phases/01-container-foundation/01-RESEARCH.md
@.planning/phases/01-container-foundation/01-01-SUMMARY.md
@tasks/setup.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Python dependency baseline</name>
  <files>python/requirements.txt</files>
  <read_first>tasks/setup.md, .env.example, .planning/phases/01-container-foundation/01-CONTEXT.md</read_first>
  <action>Create `python/requirements.txt` with the Phase 1 dependency baseline requested in `tasks/setup.md`: `demoparser2`, `pandas`, `numpy`, `scikit-learn`, `torch`, `psycopg2-binary`, `redis`, and `datasets[huggingface]` or the correct Hugging Face datasets extra form supported by pip. Prefer pinned lower bounds or exact pins that are reasonable for Python 3.12. Include `python-json-logger` if useful for future structured logs. Avoid placeholder comments.</action>
  <verify>`Select-String -Path python/requirements.txt -Pattern 'demoparser2','pandas','numpy','scikit-learn','torch','psycopg2-binary','redis','datasets'`</verify>
  <acceptance_criteria>
    - Requirements include all libraries named in the source brief.
    - Requirements are compatible with Python 3.12.
    - File contains no `TODO` placeholder text.
  </acceptance_criteria>
  <done>Python dependency baseline exists for the worker image.</done>
</task>

<task type="auto">
  <name>Task 2: Create Python 3.12 non-root worker Dockerfile</name>
  <files>docker/python/Dockerfile</files>
  <read_first>python/requirements.txt, docker-compose.yml, .env.example, tasks/setup.md</read_first>
  <action>Create `docker/python/Dockerfile` based on `python:3.12-slim`. Install system packages needed for Python scientific and PostgreSQL dependencies, such as `build-essential`, `gcc`, `libpq-dev`, and `curl`, while cleaning apt lists. Set `PYTHONDONTWRITEBYTECODE=1` and `PYTHONUNBUFFERED=1`. Add build args `APP_UID=1000` and `APP_GID=1000`, create app group/user, set `WORKDIR /app`, install requirements from `/app/requirements.txt` or copied requirements, create `/storage/demos`, chown app-owned writable paths, and end with `USER app`. Use command `python worker.py`.</action>
  <verify>`Select-String -Path docker/python/Dockerfile -Pattern 'python:3.12-slim','pip install','USER app','CMD'`</verify>
  <acceptance_criteria>
    - Dockerfile starts from `python:3.12-slim`.
    - Dockerfile installs `python/requirements.txt`.
    - Dockerfile creates and switches to non-root `app` user.
    - Dockerfile prepares `/storage/demos` for demo storage.
  </acceptance_criteria>
  <done>Python worker image is buildable and non-root.</done>
</task>

<task type="auto">
  <name>Task 3: Add minimal worker entrypoint and storage placeholders</name>
  <files>python/worker.py, data/demo-storage/.gitkeep, data/recoil_patterns/.gitkeep</files>
  <read_first>.env.example, docker-compose.yml, tasks/setup.md</read_first>
  <action>Create a minimal `python/worker.py` that can start inside the container without connecting to Redis yet. It should read `PYTHON_WORKER_QUEUE`, `REDIS_URL`, and `DEMO_STORAGE_PATH` from environment variables, print one structured JSON startup line to stdout, and then exit cleanly or sleep only if an explicit environment variable such as `WORKER_IDLE_ON_START=true` is set. This is a Phase 1 runtime smoke entrypoint, not the full BRPOP worker from Phase 3. Ensure `data/demo-storage/.gitkeep` and `data/recoil_patterns/.gitkeep` exist.</action>
  <verify>`python -m py_compile python/worker.py; Select-String -Path python/worker.py -Pattern 'PYTHON_WORKER_QUEUE','DEMO_STORAGE_PATH','json.dumps'`</verify>
  <acceptance_criteria>
    - `python/worker.py` compiles.
    - Worker reads queue and storage environment variables.
    - Worker emits structured JSON.
    - Worker does not pretend to implement Phase 3 BRPOP behavior.
  </acceptance_criteria>
  <done>Python service has a harmless smoke entrypoint for Phase 1.</done>
</task>

</tasks>

<threat_model>
Trust boundaries:
- Python process reads environment configuration and local demo storage.
- Future queue messages will cross Redis into Python, but full queue processing is out of scope for Phase 1.

STRIDE register:
- T-07 Tampering: Future demo files could be modified through writable mounts. Mitigation: Phase 1 only establishes local dev storage; later phases validate uploads and parser inputs.
- T-08 Information disclosure: Worker logs could expose secrets if it dumps full environment. Mitigation: startup log must include only non-sensitive queue/storage metadata.
- T-09 Elevation of privilege: Python service could run as root. Mitigation: Dockerfile creates app user and ends with `USER app`.
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `python -m py_compile python/worker.py` passes.
- [ ] `docker/python/Dockerfile` contains `USER app`.
- [ ] `python/requirements.txt` contains all requested Python baseline dependencies.
- [ ] `docker compose --env-file .env.example config` remains valid after this plan.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Python artifacts satisfy INFR-01, INFR-03, and INFR-05.
- Phase decisions D-05, D-06, D-07, and D-08 are visible in Python runtime configuration.

</success_criteria>

<output>
After completion, create `.planning/phases/01-container-foundation/01-03-SUMMARY.md`
</output>
