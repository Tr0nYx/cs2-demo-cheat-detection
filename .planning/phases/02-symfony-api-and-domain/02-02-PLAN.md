---
phase: 02-symfony-api-and-domain
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: ["symfony/src/Domain/Demo/*.php", "symfony/src/Domain/Player/*.php", "symfony/src/Domain/Analysis/*.php", "symfony/src/Infrastructure/Persistence/*.php", "symfony/migrations/*.php", "symfony/tests/Domain/*.php"]
autonomous: true
requirements: ["BACK-04"]
user_setup: []
must_haves:
  truths:
    - "D-07: Demo and AnalysisResult API identities are UUIDs."
    - "D-08: Player.steamId is the unique natural player identifier."
    - "D-09: Demo statuses are uploaded, queued, processing, done, and error."
    - "D-10: Store one AnalysisResult per Demo + Player."
    - "D-11: Overall label and score are first-class fields; feature scores and support data are JSON."
    - "D-12: Add practical database constraints immediately."
  artifacts:
    - path: "symfony/src/Domain/Demo/Demo.php"
      provides: "Demo aggregate/entity"
      contains: "DemoStatus"
    - path: "symfony/src/Domain/Player/Player.php"
      provides: "Player entity with unique Steam ID"
      contains: "steamId"
    - path: "symfony/src/Domain/Analysis/AnalysisResult.php"
      provides: "Player-level analysis result entity"
      contains: "overallSuspicion"
    - path: "symfony/migrations/*.php"
      provides: "PostgreSQL schema for demo, player, and analysis_result"
      contains: "analysis_result"
  key_links:
    - from: "symfony/src/Domain/Analysis/AnalysisResult.php"
      to: "symfony/src/Domain/Demo/Demo.php"
      via: "Many-to-one Demo association"
      pattern: "demo"
    - from: "symfony/src/Domain/Analysis/AnalysisResult.php"
      to: "symfony/src/Domain/Player/Player.php"
      via: "Many-to-one Player association"
      pattern: "player"
---

<objective>
Create the Doctrine-backed domain model for demos, players, and player-level analysis results.

Purpose: establish the persisted source of truth that upload, status, history, queue, and ingest flows use.
Output: entities/enums, repositories, migrations, constraints, and focused domain/schema tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-symfony-api-and-domain/02-CONTEXT.md
@.planning/phases/02-symfony-api-and-domain/02-RESEARCH.md
@.planning/phases/02-symfony-api-and-domain/02-VALIDATION.md
@.planning/phases/02-symfony-api-and-domain/02-01-SUMMARY.md
@tasks/setup.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add domain enums and entities</name>
  <files>symfony/src/Domain/Demo/Demo.php, symfony/src/Domain/Demo/DemoStatus.php, symfony/src/Domain/Player/Player.php, symfony/src/Domain/Analysis/AnalysisResult.php, symfony/src/Domain/Analysis/SuspicionLabel.php</files>
  <read_first>.planning/phases/02-symfony-api-and-domain/02-CONTEXT.md, tasks/setup.md</read_first>
  <action>Create Doctrine entities with strict types and readable domain methods. Demo fields must include UUID id, nullable `steamMatchId`, original filename metadata, stored file path, storage disk, status enum, uploadedAt, processedAt, and errorMessage. Player fields must include UUID id, unique steamId, nullable or non-empty displayName according to practical API needs. AnalysisResult fields must include UUID id, Demo, Player, roundCount, individual feature scores, overallSuspicion, suspicionLabel enum, featureData JSON, supportData/explanations JSON if useful, and analyzedAt. Use status values from D-09, not the older source-brief pending/failed names.</action>
  <verify>`php bin/console doctrine:mapping:info`; targeted PHPUnit tests for enum values and entity invariants</verify>
  <acceptance_criteria>
    - Demo status enum contains exactly `uploaded`, `queued`, `processing`, `done`, and `error`.
    - Suspicion label enum contains `clean`, `suspicious`, and `likely_cheating`.
    - Entities do not describe labels as proof or ban decisions.
  </acceptance_criteria>
  <done>Domain objects represent Phase 2 persistence needs and project language safely.</done>
</task>

<task type="auto">
  <name>Task 2: Add repositories and persistence adapters</name>
  <files>symfony/src/Infrastructure/Persistence/DemoRepository.php, symfony/src/Infrastructure/Persistence/PlayerRepository.php, symfony/src/Infrastructure/Persistence/AnalysisResultRepository.php</files>
  <read_first>symfony/config/packages/doctrine.yaml, .planning/phases/02-symfony-api-and-domain/02-CONTEXT.md</read_first>
  <action>Create Doctrine service repositories with methods needed by later plans: save/remove Demo, find Demo by UUID string, find or create Player by Steam ID, list newest AnalysisResult history by Steam ID with limit/offset, and upsert or replace result for Demo + Player while respecting the unique constraint. Keep query methods bounded and deterministic.</action>
  <verify>Repository integration tests against the test database or Doctrine schema validation</verify>
  <acceptance_criteria>
    - Player lookup is based on unique `steamId`.
    - History query returns newest analyzed results first.
    - Repository APIs do not expose raw SQL to controllers.
  </acceptance_criteria>
  <done>Application services can use repositories without coupling controllers to Doctrine internals.</done>
</task>

<task type="auto">
  <name>Task 3: Generate and validate migration</name>
  <files>symfony/migrations/*.php</files>
  <read_first>symfony/src/Domain/Demo/Demo.php, symfony/src/Domain/Player/Player.php, symfony/src/Domain/Analysis/AnalysisResult.php</read_first>
  <action>Create a Doctrine migration for PostgreSQL. The schema must include unique `player.steam_id`, unique analysis result per `(demo_id, player_id)`, non-null required fields, JSON columns for feature/support data, indexes for demo status, uploaded/analyzed timestamps, and player history lookup. Prefer enum strings with application-level PHP enum validation unless PostgreSQL enum types are already a local pattern.</action>
  <verify>`php bin/console doctrine:migrations:migrate --no-interaction`; `php bin/console doctrine:schema:validate --skip-sync` or full schema validation when safe</verify>
  <acceptance_criteria>
    - Migration applies cleanly on the Phase 1 PostgreSQL service.
    - Database constraints enforce D-08, D-10, and D-12.
    - Rollback is defined by the migration `down()` method.
  </acceptance_criteria>
  <done>PostgreSQL has the Phase 2 schema needed by API and ingest flows.</done>
</task>

</tasks>

<threat_model>
Trust boundaries:
- Domain data crosses from HTTP requests and future Python output into PostgreSQL.
- Suspicion scores are research signals and may be displayed to users later.

STRIDE register:
- T-12 Tampering: Duplicate or inconsistent result rows could distort player history. Mitigation: unique `(demo_id, player_id)` constraint.
- T-13 Repudiation: Missing timestamps make analysis state hard to audit. Mitigation: store uploadedAt and analyzedAt/processedAt.
- T-14 Information disclosure: Raw support JSON could later contain sensitive internals. Mitigation: keep Phase 2 fields structured and avoid storing exception traces in public fields.
</threat_model>

<verification>
Before declaring plan complete:
- [ ] Doctrine mapping info succeeds.
- [ ] Migration applies cleanly to PostgreSQL.
- [ ] Doctrine schema validation passes or documented sync-only warnings are harmless.
- [ ] Tests cover enum values, unique Steam ID, and unique Demo + Player result behavior.
</verification>

<success_criteria>

- All tasks completed.
- BACK-04 is materially implemented.
- Database constraints match D-08, D-10, and D-12.
- The domain model remains independent from Python parsing/scoring implementation.

</success_criteria>

<output>
After completion, create `.planning/phases/02-symfony-api-and-domain/02-02-SUMMARY.md`
</output>
