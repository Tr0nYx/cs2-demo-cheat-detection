---
phase: 13-demo-viewer-heatmap
plan: 03
type: execute
wave: 3
depends_on: ["13-01", "13-02"]
files_modified:
  - symfony/src/Application/Command/GenerateHeatmapMessage.php
  - symfony/src/Application/Handler/GenerateHeatmapHandler.php
  - symfony/src/Infrastructure/Cache/DemoHeatmapCacheRepository.php
  - symfony/src/Infrastructure/Queue/RedisViewerJobPublisher.php
  - symfony/src/Presentation/Controller/DemoViewerController.php
  - python/viewer/worker_viewer.py
  - python/viewer/heatmap_job.py
  - Makefile
  - symfony/tests/Presentation/Controller/DemoHeatmapControllerTest.php
  - python/tests/test_viewer_worker_heatmap.py
autonomous: true
requirements: [VIEWER-HEATMAP-END_TO_END]
user_setup:
  - "Radar PNG assets should be available under assets/maps/{map_name}_radar.png for real heatmap rendering."
must_haves:
  truths:
    - "GET /api/demos/{id}/heatmap serves image/png when cached or generated"
    - "Heatmap cache key includes demo, player/all, type, and round filter"
    - "Heatmap PNG responses set Cache-Control public max-age=604800"
    - "Cache miss queues a Python viewer/heatmap job instead of doing long rendering in the request path"
    - "Python viewer worker consumes Redis viewer jobs with structured JSON logs and graceful shutdown"
    - "Makefile exposes heatmaps, viewer-worker, and export-ticks helpers"
  artifacts:
    - path: "symfony/src/Presentation/Controller/DemoViewerController.php"
      provides: "Heatmap image endpoint"
      min_lines: 220
    - path: "symfony/src/Infrastructure/Cache/DemoHeatmapCacheRepository.php"
      provides: "Redis/file cache lookup for generated PNGs"
      min_lines: 90
    - path: "python/viewer/worker_viewer.py"
      provides: "Viewer job worker for heatmaps and tick export jobs"
      min_lines: 140
    - path: "python/viewer/heatmap_job.py"
      provides: "Job handler that gathers events, renders PNG, writes cache/file"
      min_lines: 120
  key_links:
    - from: "DemoViewerController::heatmap"
      to: "GenerateHeatmapHandler"
      via: "message/handler dispatch"
      pattern: "GenerateHeatmapMessage"
    - from: "GenerateHeatmapHandler"
      to: "RedisViewerJobPublisher"
      via: "Redis queue"
      pattern: "cs2.viewer"
---

<objective>
Close the heatmap path end-to-end.

This wave adds heatmap cache lookup, `GET /api/demos/{id}/heatmap`, Redis job dispatch, and a Python viewer worker that renders PNG heatmaps from persisted/demo-derived events.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-demo-viewer-heatmap/13-CONTEXT.md
@.planning/phases/13-demo-viewer-heatmap/13-RESEARCH.md
@.planning/phases/13-demo-viewer-heatmap/13-PATTERNS.md
@.planning/phases/13-demo-viewer-heatmap/13-01-SUMMARY.md
@.planning/phases/13-demo-viewer-heatmap/13-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Symfony heatmap cache and queue publisher</name>
  <files>
    symfony/src/Infrastructure/Cache/DemoHeatmapCacheRepository.php
    symfony/src/Infrastructure/Queue/RedisViewerJobPublisher.php
  </files>
  <read_first>
    symfony/src/Infrastructure/Cache/DemoTickCacheRepository.php
    symfony/src/Infrastructure/Queue/RedisAnalysisJobPublisher.php
  </read_first>
  <action>
Create a heatmap cache repository with:
- Redis key `heatmap:{demoId}:{playerOrAll}:{type}:{roundFilter}`.
- `fetchBytes(...)`, `storeBytes(...)`, `filePathFor(...)`, `fetchFile(...)`.
- TTL 7 days.
- filesystem root from env/config, default `/storage/heatmaps`.

Create a viewer job publisher with:
- env `REDIS_URL`.
- env/default queue `PYTHON_VIEWER_QUEUE=cs2.viewer`.
- method `publish(array $payload): void`.
  </action>
  <verify>
    <automated>grep -c "class DemoHeatmapCacheRepository" symfony/src/Infrastructure/Cache/DemoHeatmapCacheRepository.php</automated>
    <automated>grep -c "class RedisViewerJobPublisher" symfony/src/Infrastructure/Queue/RedisViewerJobPublisher.php</automated>
  </verify>
  <done>Symfony can check/store heatmap PNG cache and enqueue viewer jobs.</done>
</task>

<task type="auto">
  <name>Task 2: Add heatmap message/handler and endpoint</name>
  <files>
    symfony/src/Application/Command/GenerateHeatmapMessage.php
    symfony/src/Application/Handler/GenerateHeatmapHandler.php
    symfony/src/Presentation/Controller/DemoViewerController.php
    symfony/tests/Presentation/Controller/DemoHeatmapControllerTest.php
  </files>
  <read_first>
    symfony/src/Presentation/Controller/DemoViewerController.php
    symfony/src/Application/Command/CLAUDE.md
    symfony/src/Application/Handler/CLAUDE.md
  </read_first>
  <action>
Add `GET /api/demos/{id}/heatmap`:
- query params: `type`, `player`, `round_from`, `round_to`.
- validate heatmap type is kills/deaths/damage/taken/grenades.
- validate round range if present.
- cache first: Redis bytes, then file reference.
- on cache/file hit: return `Response` with `Content-Type: image/png` and `Cache-Control: public, max-age=604800`.
- on miss: dispatch/enqueue `GenerateHeatmapMessage` with demoId, playerId/playerSteamId, heatmapType, roundFrom, roundTo.
- return `202` JSON `{status:"generating", retryAfterSeconds:...}` on miss unless synchronous render is already available and safely under request budget.

Tests cover cache hit, file hit, miss queues job, invalid type, invalid rounds, and headers.
  </action>
  <verify>
    <automated>cd symfony && php bin/phpunit tests/Presentation/Controller/DemoHeatmapControllerTest.php -x</automated>
  </verify>
  <done>Heatmap endpoint returns PNG on hit and queues generation on miss.</done>
</task>

<task type="auto">
  <name>Task 3: Add Python viewer worker and heatmap job handler</name>
  <files>
    python/viewer/worker_viewer.py
    python/viewer/heatmap_job.py
    python/tests/test_viewer_worker_heatmap.py
  </files>
  <read_first>
    python/worker.py
    python/viewer/heatmap.py
    python/viewer/tick_exporter.py
  </read_first>
  <action>
Create `worker_viewer.py` modeled after `python/worker.py`:
- env `PYTHON_VIEWER_QUEUE`, default `cs2.viewer`.
- Redis BRPOP loop.
- structured JSON logging.
- SIGTERM/SIGINT graceful shutdown.
- supports job types `generate_heatmap` and `export_ticks`.

Create `heatmap_job.py`:
- validates job payload.
- loads event data from PostgreSQL or accepts event payload depending on available repository seam.
- calls `render_heatmap`.
- writes PNG bytes to Redis and `/storage/heatmaps/{demoId}/...png`.
- emits completion/error event if Symfony expects one.

Tests should fake Redis/DB where possible and verify job dispatch path, output file path, and structured log events.
  </action>
  <verify>
    <automated>cd python && pytest tests/test_viewer_worker_heatmap.py -q</automated>
  </verify>
  <done>Python worker can render heatmap jobs outside the HTTP request path.</done>
</task>

<task type="auto">
  <name>Task 4: Add Makefile helpers</name>
  <files>
    Makefile
  </files>
  <read_first>
    Makefile
  </read_first>
  <action>
Add targets:
- `heatmaps demo=UUID`: enqueue or run all heatmaps for one demo.
- `viewer-worker`: start Python viewer worker.
- `export-ticks demo=UUID`: export tick cache for one demo.

Follow existing Makefile style and environment variable names.
  </action>
  <verify>
    <automated>Get-Content Makefile | Select-String -Pattern "viewer-worker|heatmaps|export-ticks"</automated>
  </verify>
  <done>Developer workflow can run viewer worker, heatmap generation, and tick export.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> heatmap endpoint | User controls query params |
| Symfony -> Redis queue | Internal job payload boundary |
| Python worker -> filesystem | Generated PNGs written to storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-09 | Denial of Service | heatmap endpoint | mitigate | Cache first, queue on miss, validate type/range |
| T-13-10 | Tampering | file path | mitigate | Derive paths from sanitized IDs and fixed storage root |
| T-13-11 | Information Disclosure | heatmap PNG | accept | PNG visualizes post-game demo events; no secrets |
| T-13-12 | Denial of Service | Python renderer | mitigate | async worker, structured failures, no request-thread rendering requirement |
</threat_model>

<verification>
Run:

```bash
cd symfony && php bin/phpunit tests/Presentation/Controller/DemoHeatmapControllerTest.php -x
cd python && pytest tests/test_viewer_worker_heatmap.py -q
```
</verification>

<success_criteria>
- Heatmap endpoint cache hit returns PNG with 7-day cache header.
- Cache miss queues a background render job.
- Python viewer worker renders and stores heatmaps.
- Makefile has the three requested helper targets.
</success_criteria>

<output>
After completion, create `.planning/phases/13-demo-viewer-heatmap/13-03-SUMMARY.md` with endpoint behavior, worker queue contract, cache keys, Makefile targets, and tests.
</output>
