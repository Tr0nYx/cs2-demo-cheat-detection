---
phase: 18
plan: 04
type: implementation
wave: 4
depends_on: ["18-02-PLAN.md", "18-03-PLAN.md"]
files_modified:
  - frontend/lib/hooks/useSteamMatchHistory.ts
  - frontend/components/SteamMatchHistoryCard.tsx
  - frontend/app/dashboard/page.tsx
  - frontend/__tests__/components/SteamMatchHistoryCard.test.tsx
  - frontend/__tests__/hooks/useSteamMatchHistory.test.tsx
  - symfony/tests/UI/Api/SteamMatchHistoryControllerTest.php
autonomous: true
requirements_addressed: []
---

# 18-04-PLAN: Dashboard Tracking UI

<objective>
Add a dashboard setup/status panel for connecting Valve match-history tracking, showing minimal safe status, and disconnecting without exposing the Steam Game Authentication Code.

Covers decisions: D-01, D-03, D-04, D-11, D-12, D-13, D-16, D-17, D-18, D-19.
</objective>

<context>
Read first:
- `.planning/phases/18-sharecode-match-history-tracking/18-CONTEXT.md`
- `.planning/phases/18-sharecode-match-history-tracking/18-RESEARCH.md`
- `frontend/app/dashboard/page.tsx`
- `frontend/components/UserProfile.tsx`
- `frontend/components/QuickUploadCard.tsx`
- `frontend/lib/hooks/useImportSharecode.ts`
- `symfony/src/UI/Api/SteamMatchHistoryController.php`
</context>

<tasks>
<task>
  <id>18-04-01</id>
  <title>Add frontend API hook</title>
  <type>implementation</type>
  <files>
    - frontend/lib/hooks/useSteamMatchHistory.ts
    - frontend/__tests__/hooks/useSteamMatchHistory.test.tsx
  </files>
  <action>
    Add a React Query hook for fetching tracking status, connecting with `{ steamidkey, seed }`, and disconnecting. Keep response types aligned with the safe backend DTO and never retain/show the plaintext key after mutation settles.
  </action>
  <acceptance_criteria>
    - Hook exposes status, connect mutation, and disconnect mutation.
    - Connect request sends only `steamidkey` and seed input; no free `steamid` field.
    - Query invalidates/refetches after connect/disconnect.
    - Tests cover success and error states.
  </acceptance_criteria>
  <verify>
    cd frontend; npm test -- SteamMatchHistory
  </verify>
</task>

<task>
  <id>18-04-02</id>
  <title>Build dashboard tracking card</title>
  <type>implementation</type>
  <files>
    - frontend/components/SteamMatchHistoryCard.tsx
    - frontend/__tests__/components/SteamMatchHistoryCard.test.tsx
  </files>
  <action>
    Build a compact dashboard card with a setup form when disconnected and a minimal status view when connected. Inputs: Steam Game Authentication Code and seed sharecode/link. Status view: connected since, last check, next check if available, status, shortened known code, discovered count, queued/imported count, and disconnect action.
  </action>
  <acceptance_criteria>
    - Card accepts plain sharecode and supported Steam launcher link input.
    - Plaintext `steamidkey` field clears after successful connect and is never displayed in status.
    - Status states render distinct copy for active, caught up, invalid seed, auth failed, rate limited, Steam unavailable, and disconnected.
    - Disconnect requires an explicit button action and updates status.
    - No visible text frames match-history metadata as proof of cheating.
  </acceptance_criteria>
  <verify>
    cd frontend; npm test -- SteamMatchHistoryCard
  </verify>
</task>

<task>
  <id>18-04-03</id>
  <title>Integrate card into dashboard</title>
  <type>implementation</type>
  <files>
    - frontend/app/dashboard/page.tsx
  </files>
  <action>
    Place the tracking card in the authenticated dashboard setup area, near user profile/upload controls but not inside another card. Preserve existing dashboard filters, demo history, and quick upload behavior.
  </action>
  <acceptance_criteria>
    - Authenticated dashboard displays the card.
    - Existing `UserProfile`, filtered scope, `DemoHistoryTable`, and `QuickUploadCard` remain usable.
    - Layout stays responsive at mobile and desktop widths.
  </acceptance_criteria>
  <verify>
    cd frontend; npm test -- SteamMatchHistoryCard
  </verify>
</task>

<task>
  <id>18-04-04</id>
  <title>Add end-to-end API/UI safety assertions</title>
  <type>implementation</type>
  <files>
    - frontend/__tests__/components/SteamMatchHistoryCard.test.tsx
    - symfony/tests/UI/Api/SteamMatchHistoryControllerTest.php
  </files>
  <action>
    Add tests asserting that neither API responses nor rendered status contain the full `steamidkey`, encrypted ciphertext, or a user-entered Steam ID field. Cover the user-supplied example launcher link through the UI flow.
  </action>
  <acceptance_criteria>
    - Tests fail if response/rendered status includes credential material.
    - Tests cover the example `steam://rungame/.../+csgo_download_match%20CSGO-...` input.
    - Tests verify disconnect preserves safe status and does not imply analysis deletion.
  </acceptance_criteria>
  <verify>
    cd frontend; npm test -- SteamMatchHistoryCard
    cd symfony; php bin/phpunit --filter SteamMatchHistoryControllerTest
  </verify>
</task>
</tasks>

<threat_model>
T-18-07: The dashboard could expose the stored Game Authentication Code through status rendering, browser state, or error text.
Mitigation: clear form state after connect, render only safe DTO fields, add tests for absent plaintext/ciphertext, and avoid putting secrets in URLs.

T-18-08: UI copy could imply auto-discovered match history is a cheating signal.
Mitigation: describe it as import/provenance automation only and keep suspicion/TRACE language tied to demo analysis results.
</threat_model>

<verification>
- `cd frontend; npm test -- SteamMatchHistory`
- `cd frontend; npm test -- SteamMatchHistoryCard`
- `cd symfony; php bin/phpunit --filter SteamMatchHistoryControllerTest`
</verification>

<success_criteria>
- Dashboard provides a clear consent-based setup and status view.
- Users can connect and disconnect match-history tracking.
- UI never displays `steamidkey` after submission.
- Existing dashboard workflows remain intact.
</success_criteria>
