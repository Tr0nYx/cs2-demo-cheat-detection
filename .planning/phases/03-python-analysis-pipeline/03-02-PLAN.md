---
phase: 03
plan: 02
type: auto
wave: 2
depends_on:
  - 03-01
files_modified:
  - python/parser/__init__.py
  - python/parser/types.py
  - python/parser/adapter.py
autonomous: true
requirements:
  - FEAT-01
  - FEAT-02
user_setup: []

must_haves:
  truths:
    - "Parser adapter wraps demoparser2 and validates tick DataFrame structure"
    - "Parser adapter validates event DataFrame structure and presence"
    - "Parser adapter raises DemoParseError with clear message on validation failure"
    - "Parser returns ParsedDemo dataclass containing ticks_df and events_df"
    - "Extracted ticks include all required properties: X, Y, Z, pitch, yaw, velocity_X/Y/Z, health, armor_value, is_shooting, is_scoped, is_airborne, active_weapon_name, ping"
    - "Extracted events include all types: player_death, weapon_fire, player_footstep, player_jump, player_land, round_start, round_end"
  artifacts:
    - path: "python/parser/__init__.py"
      provides: "Module initialization, exports ParsedDemo and DemoParseError"
      min_lines: 5
    - path: "python/parser/types.py"
      provides: "ParsedDemo dataclass and DemoParseError exception"
      min_lines: 20
    - path: "python/parser/adapter.py"
      provides: "DemoParserAdapter wrapping demoparser2"
      min_lines: 100
  key_links:
    - from: "python/parser/adapter.py"
      to: "demoparser2"
      via: "DemoParser(file_path) initialization"
      pattern: "DemoParser|parse_ticks|parse_event"
    - from: "python/worker.py"
      to: "python/parser/adapter.py"
      via: "parse_demo(file_path) -> ParsedDemo"
      pattern: "DemoParserAdapter|parse_demo"
---

<objective>
Implement DemoParserAdapter that wraps demoparser2, extracts requested tick properties and events, validates data structure, and surfaces clear parsing errors. This is the clean boundary between demoparser2 quirks and feature extractor assumptions.

Purpose: Feature extractors depend on validated, structured demo data. This adapter ensures consistency and handles demoparser2 edge cases.

Output: Parser module with ParsedDemo contract, DemoParseError exception, and DemoParserAdapter class ready for feature extractors in Tasks 03-03 and 03-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-python-analysis-pipeline/03-CONTEXT.md
@.planning/phases/03-python-analysis-pipeline/03-RESEARCH.md
@python/requirements.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create parser module structure and type definitions</name>
  <files>python/parser/__init__.py, python/parser/types.py</files>
  <read_first>
    - python/requirements.txt (verify pandas and demoparser2 present)
    - .planning/phases/03-python-analysis-pipeline/03-CONTEXT.md (D-01 through D-05)
    - .planning/phases/03-python-analysis-pipeline/03-RESEARCH.md (Pattern 1: DemoParserAdapter)
  </read_first>
  <action>
Create python/parser/__init__.py with:
  - from .types import ParsedDemo, DemoParseError
  - from .adapter import DemoParserAdapter
  - __all__ = ["ParsedDemo", "DemoParseError", "DemoParserAdapter"]

Create python/parser/types.py with:
  1. ParsedDemo dataclass (per D-01):
     - Imports: from dataclasses import dataclass; import pandas as pd
     - Fields:
       - ticks_df: pd.DataFrame (with columns: tick, steamid, X, Y, Z, pitch, yaw, velocity_X, velocity_Y, velocity_Z, health, armor_value, is_shooting, is_scoped, is_airborne, active_weapon_name, ping)
       - events_df: pd.DataFrame (with columns: tick, event_type, and event-specific columns)
     - Docstring: "Validated tick and event data from a CS2 demo"

  2. DemoParseError exception (per D-05):
     - Inherits from Exception
     - Custom exception for demo parsing failures
     - Docstring: "Raised when demo file cannot be parsed or is invalid"

Example from RESEARCH.md (use as reference):
```python
from dataclasses import dataclass
import pandas as pd

@dataclass
class ParsedDemo:
    """Validated tick and event data from a CS2 demo."""
    ticks_df: pd.DataFrame
    events_df: pd.DataFrame

class DemoParseError(Exception):
    """Raised when demo file cannot be parsed or is invalid."""
    pass
```

Both should be simple, focused type definitions with minimal logic.
  </action>
  <verify>
    - python/parser/__init__.py exists and exports ParsedDemo, DemoParseError, DemoParserAdapter
    - python/parser/types.py contains dataclass ParsedDemo with ticks_df and events_df fields
    - python/parser/types.py contains class DemoParseError(Exception)
    - No business logic in types.py (only type definitions)
    - grep "from dataclasses import dataclass" python/parser/types.py >= 1
    - grep "class ParsedDemo" python/parser/types.py >= 1
    - grep "class DemoParseError" python/parser/types.py >= 1
  </verify>
  <done>Parser module structure and type definitions complete, ready for adapter implementation</done>
</task>

<task type="auto">
  <name>Task 2: Implement DemoParserAdapter with validation and error handling</name>
  <files>python/parser/adapter.py</files>
  <read_first>
    - python/requirements.txt (demoparser2>=0.37.0, pandas>=2.2.0)
    - python/parser/types.py (ParsedDemo, DemoParseError)
    - .planning/phases/03-python-analysis-pipeline/03-CONTEXT.md (D-01 through D-05: all parser decisions)
    - .planning/phases/03-python-analysis-pipeline/03-RESEARCH.md (Pattern 1: full DemoParserAdapter example code; Common Pitfalls #1 and #2)
  </read_first>
  <action>
Create python/parser/adapter.py with DemoParserAdapter class implementing:

1. REQUIRED_TICK_COLS constant (per D-02):
   List of 16 tick properties to extract: "tick", "steamid", "X", "Y", "Z", "pitch", "yaw", "velocity_X", "velocity_Y", "velocity_Z", "health", "armor_value", "is_shooting", "is_scoped", "is_airborne", "active_weapon_name", "ping"

2. REQUIRED_EVENT_TYPES constant (per D-03):
   List of 7 event types to extract: "player_death", "weapon_fire", "player_footstep", "player_jump", "player_land", "round_start", "round_end"

3. parse_demo(file_path: str) -> ParsedDemo method (per D-01, D-04, D-05):

   Error handling (all-or-nothing per D-05):
   a. File not found: catch FileNotFoundError → raise DemoParseError(f"Demo file not found: {file_path}")
   b. Parser initialization fails: catch Exception → raise DemoParseError(f"Failed to initialize parser: {e}")
   c. Tick parsing fails: catch Exception → raise DemoParseError(f"Failed to parse ticks: {e}")
   d. Ticks DataFrame is None or empty: raise DemoParseError("Parser returned empty ticks DataFrame")
   e. Missing required tick columns: raise DemoParseError(f"Missing tick columns: {missing_cols}")
   f. Tick ordering is not sequential: check if (ticks_df["tick"].diff().dropna() > 0).all() else raise DemoParseError("Ticks are not ordered by tick_number")
   g. No events extracted: raise DemoParseError("No gameplay events extracted from demo")

   Implementation:
   - from demoparser2 import DemoParser (note: not demoparser2.DemoParser)
   - Initialize: parser = DemoParser(file_path)
   - Extract ticks: ticks_df = parser.parse_ticks(self.REQUIRED_TICK_COLS) with all validation above
   - Extract events: iterate over REQUIRED_EVENT_TYPES, call parser.parse_event(event_type) for each
     - If parse_event returns None or empty, skip (some events may not exist in a demo)
     - If parse_event succeeds, add column "event_type" = event_type to the event_df
     - Collect all non-empty event DataFrames
   - If no events collected, raise DemoParseError
   - Concatenate all event DataFrames: events_df = pd.concat(events_list, ignore_index=True)
   - Return ParsedDemo(ticks_df=ticks_df, events_df=events_df)

   Docstring: "Parse a CS2 demo file and return validated tick/event data. Raises DemoParseError if file cannot be opened, ticks/events are empty, or required columns are missing."

Example from RESEARCH.md (full implementation provided):
[See RESEARCH.md Pattern 1 for complete code — copy structure verbatim, adapt to this class]

Quality requirements (per CONTEXT.md and RESEARCH.md):
- All exceptions converted to DemoParseError (never propagate demoparser2 exceptions)
- Shallow validation per D-04: check structure, not deep CS2 format validation
- Clear error messages with context (file path, missing columns)
- No silent empty data (raise exception, never return empty DataFrame silently per Common Pitfall #1)

4. Type hints and imports:
   - from __future__ import annotations (for forward references)
   - from demoparser2 import DemoParser
   - import pandas as pd
   - from .types import ParsedDemo, DemoParseError

Do NOT implement feature extraction or scoring in this task. This is purely parsing and validation.
  </action>
  <verify>
    - python/parser/adapter.py exists and contains class DemoParserAdapter
    - REQUIRED_TICK_COLS contains all 16 properties (X, Y, Z, pitch, yaw, velocity_X/Y/Z, health, armor_value, is_shooting, is_scoped, is_airborne, active_weapon_name, ping)
    - REQUIRED_EVENT_TYPES contains all 7 event types (player_death, weapon_fire, player_footstep, player_jump, player_land, round_start, round_end)
    - parse_demo method accepts file_path: str and returns ParsedDemo
    - All exception handling converts to DemoParseError (grep "raise DemoParseError" python/parser/adapter.py >= 6)
    - Tick ordering validation present (grep "diff()" python/parser/adapter.py >= 1)
    - Event concatenation using pd.concat (grep "pd.concat" python/parser/adapter.py >= 1)
    - No silent empty returns (all empty cases raise DemoParseError)
    - From statements import ParsedDemo and DemoParseError from .types
  </verify>
  <done>
DemoParserAdapter is complete, validates tick/event structure, surfaces clear errors, and returns ParsedDemo contract. Ready for integration with worker loop and feature extractors.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File system → Parser | Untrusted demo files from storage; could be corrupt, truncated, or missing required data |
| Parser output → Feature Extractors | Feature extractors depend on validated DataFrames; invalid structure breaks downstream code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering | demoparser2 malformed input causes crash | mitigate | DemoParserAdapter wraps all demoparser2 calls in try/except; converts exceptions to DemoParseError. Worker catches and logs, marks demo as error. Per D-05. |
| T-03-02-02 | Denial of Service | Corrupt demo file causes parser to hang or crash | mitigate | demoparser2 is hardened; parser initialization has timeout semantics built into Rust bindings. If crash occurs, worker exit code 1 triggers orchestration restart. Phase 1 container nonroot user prevents system impact. |
| T-03-02-03 | Information Disclosure | Error messages leak internal paths or state | mitigate | Log only sanitized file paths or demo_id; do not log full file system paths in production. Per D-40. |

</threat_model>

<verification>
## Phase Requirements Coverage (Wave 2)

| Req ID | Task | Verification |
|--------|------|--------------|
| FEAT-01 | Task 2: parse_demo extracts ticks | REQUIRED_TICK_COLS contains all 16 properties; parser.parse_ticks() called with list |
| FEAT-02 | Task 2: parse_demo extracts events | REQUIRED_EVENT_TYPES contains all 7 types; parser.parse_event() called for each |

## Integration Readiness

After Task 03-02, worker.py (Task 03-01) can:
- Import DemoParserAdapter from python.parser
- Call adapter.parse_demo(file_path) in process_job()
- Catch DemoParseError and call result_writer.write_error()
- Continue to feature extraction with valid ParsedDemo

## Success Criteria

After execution of Task 03-02:
1. `python/parser/adapter.py` exists with DemoParserAdapter class (~100+ lines)
2. Parser can be instantiated: `adapter = DemoParserAdapter()`
3. parse_demo() can be called with a valid demo file and returns ParsedDemo
4. parse_demo() raises DemoParseError on file not found, corrupt structure, missing columns
5. Error messages are clear and include context (file path, missing column names)

</verification>

<success_criteria>
Phase 3 Task 03-02 is complete when:

1. **Parser Module Structure:**
   - `python/parser/__init__.py` exports ParsedDemo, DemoParseError, DemoParserAdapter
   - `python/parser/types.py` defines ParsedDemo dataclass and DemoParseError exception
   - `python/parser/adapter.py` implements DemoParserAdapter class

2. **Tick Extraction (FEAT-01):**
   - `parse_demo()` extracts 16 required tick properties: X, Y, Z, pitch, yaw, velocity_X/Y/Z, health, armor_value, is_shooting, is_scoped, is_airborne, active_weapon_name, ping
   - Returned ticks_df includes all columns and is non-empty
   - Tick ordering is validated (sequential by tick number)

3. **Event Extraction (FEAT-02):**
   - `parse_demo()` extracts 7 required event types: player_death, weapon_fire, player_footstep, player_jump, player_land, round_start, round_end
   - Returned events_df includes all available event types and is non-empty
   - Event concatenation is correct (no duplicates, all columns preserved)

4. **Error Handling (D-05):**
   - All demoparser2 exceptions are caught and converted to DemoParseError
   - File not found, parser init failure, tick/event parsing failure → DemoParseError with clear message
   - Empty ticks/events, missing columns, invalid structure → DemoParseError
   - No silent empty returns; all error conditions raise exception

5. **Code Quality:**
   - Type hints present (file_path: str, -> ParsedDemo)
   - Docstrings for parse_demo() method
   - REQUIRED_TICK_COLS and REQUIRED_EVENT_TYPES are class constants
   - Imports: ParsedDemo and DemoParseError from .types; DemoParser from demoparser2

</success_criteria>

<output>
After completion, create `.planning/phases/03-python-analysis-pipeline/03-02-SUMMARY.md` documenting:
- ParsedDemo contract (fields, usage)
- DemoParserAdapter interface (parse_demo signature, exceptions raised)
- Validation rules applied (tick ordering, required columns, event concatenation)
- Error handling strategy (all-or-nothing exception conversion)
- Example usage: adapter = DemoParserAdapter(); parsed = adapter.parse_demo("/path/to/demo.dem")
</output>
