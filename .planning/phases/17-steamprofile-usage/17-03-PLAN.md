---
phase: 17
plan: 03
type: implementation
wave: 2
depends_on: ["17-01-PLAN.md", "17-02-PLAN.md"]
files_modified:
  - symfony/src/Application/Steam/RefreshSteamProfileMessage.php
  - symfony/src/Application/Steam/RefreshSteamProfileHandler.php
  - symfony/src/Application/Steam/SteamProfileRefreshPlanner.php
  - symfony/src/Command/RefreshSteamProfilesCommand.php
  - symfony/config/packages/messenger.yaml
  - symfony/tests/Application/Steam/SteamProfileRefreshPlannerTest.php
  - symfony/tests/Application/Steam/RefreshSteamProfileHandlerTest.php
autonomous: true
requirements_addressed: []
---

# 17-03-PLAN: Tiered Refresh Pipeline

<objective>
Queue and execute tiered Steam profile/inventory refreshes for players seen in demos, with batching, stale selection, and backoff for private/unavailable/erroring profiles.

Covers decisions: D-02, D-03, D-04, D-05, D-15, D-16.
</objective>

<context>
Read first:
- `.planning/phases/17-steamprofile-usage/17-CONTEXT.md`
- `.planning/phases/17-steamprofile-usage/17-RESEARCH.md`
- `symfony/config/packages/messenger.yaml`
- `symfony/src/Infrastructure/Persistence/PlayerRepository.php`
- `symfony/src/Application/Demo/HltvImportHandler.php`
- `symfony/src/Application/Demo/Cron/HltvTopMatchesCommand.php`
</context>

<tasks>
<task>
  <id>17-03-01</id>
  <title>Add refresh message and Messenger route</title>
  <type>implementation</type>
  <files>
    - symfony/src/Application/Steam/RefreshSteamProfileMessage.php
    - symfony/config/packages/messenger.yaml
  </files>
  <action>
    Add `RefreshSteamProfileMessage` carrying Steam ID, priority tier, reason, and optional force flag. Route it to an async Messenger transport, using an existing transport if appropriate or a dedicated Steam refresh queue if configuration already supports it.
  </action>
  <acceptance_criteria>
    - Message is routed in `messenger.yaml`.
    - Message includes tier and reason for audit logs.
  </acceptance_criteria>
  <verify>
    cd symfony; php bin/console lint:container
  </verify>
</task>

<task>
  <id>17-03-02</id>
  <title>Implement refresh tier planner</title>
  <type>implementation</type>
  <files>
    - symfony/src/Application/Steam/SteamProfileRefreshPlanner.php
    - symfony/tests/Application/Steam/SteamProfileRefreshPlannerTest.php
  </files>
  <action>
    Create logic that selects players for refresh based on latest profile/inventory snapshots, recently seen demos, active player appearances, private/unavailable states, and last error. Encode tiers such as `new`, `recent`, `active`, `stale`, `inactive`, and `backoff`.
  </action>
  <acceptance_criteria>
    - New/no-snapshot players are highest priority.
    - Recent/active players refresh before inactive players.
    - Private/unavailable/erroring profiles use increasing backoff.
    - Tests cover each tier.
  </acceptance_criteria>
  <verify>
    cd symfony; php bin/phpunit --filter SteamProfileRefreshPlanner
  </verify>
</task>

<task>
  <id>17-03-03</id>
  <title>Implement refresh handler</title>
  <type>implementation</type>
  <files>
    - symfony/src/Application/Steam/RefreshSteamProfileHandler.php
    - symfony/tests/Application/Steam/RefreshSteamProfileHandlerTest.php
  </files>
  <action>
    Handler fetches profile summary, public inventory, market prices, and writes versioned snapshots. It must log structured JSON context, persist partial successes, and never dispatch analysis/scoring changes.
  </action>
  <acceptance_criteria>
    - Profile snapshot is stored even when inventory is private/unavailable.
    - Inventory snapshot records private/error states.
    - Handler does not mutate AnalysisResult, TraceRating, or suspicion labels.
  </acceptance_criteria>
  <verify>
    cd symfony; php bin/phpunit --filter RefreshSteamProfileHandler
  </verify>
</task>

<task>
  <id>17-03-04</id>
  <title>Add console command for scheduled refresh</title>
  <type>implementation</type>
  <files>
    - symfony/src/Command/RefreshSteamProfilesCommand.php
  </files>
  <action>
    Add `app:steam:refresh-profiles` with `--limit`, `--tier`, `--force`, and `--dry-run` options. It should call the refresh planner, dispatch messages, and print counts by tier and reason.
  </action>
  <acceptance_criteria>
    - Dry-run prints candidates without dispatch.
    - Limit caps dispatched messages.
    - Command can target a tier for operational control.
  </acceptance_criteria>
  <verify>
    cd symfony; php bin/phpunit --filter RefreshSteamProfilesCommand
  </verify>
</task>
</tasks>

<threat_model>
T-17-03: Refresh jobs could overload Steam or leak private profile state through logs.
Mitigation: tiered stale selection, backoff, limits, dry-run, structured logs without raw inventory dumps.
</threat_model>

<verification>
- `cd symfony; php bin/phpunit --filter SteamProfileRefreshPlanner`
- `cd symfony; php bin/phpunit --filter RefreshSteamProfileHandler`
- `cd symfony; php bin/console app:steam:refresh-profiles --dry-run --limit=10`
</verification>

<success_criteria>
- D-02 tiered refresh is implemented.
- Refresh produces versioned snapshots and respects backoff.
- Refresh does not affect visible scoring or labels.
</success_criteria>
