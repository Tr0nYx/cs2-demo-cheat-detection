---
phase: 19
plan: 03
type: implementation
wave: 3
depends_on: ["19-01-PLAN.md", "19-02-PLAN.md"]
files_modified:
  - frontend/app/results/[id]/page.tsx
  - frontend/components/ResultsCard.tsx
  - frontend/components/DemoDetail/TraceCard.tsx
  - frontend/components/DemoDetail/TraceComponentChart.tsx
  - frontend/components/DemoDetail/TraceChart.tsx
  - frontend/components/DemoDetail/CalibrationContextCard.tsx
  - frontend/components/Analytics/SensitivityTuner.tsx
  - frontend/__tests__/components/ResultsCard.test.tsx
  - frontend/__tests__/components/DemoDetail/TraceCard.test.tsx
  - frontend/__tests__/components/Analytics/SensitivityTuner.test.tsx
autonomous: true
requirements_addressed: ["UI-01"]
---

# 19-03-PLAN: Results And TRACE Explainability Console

<objective>
Rebuild the results page as an evidence-first analysis surface with consistent research framing, clearer TRACE structure, neutral sensitivity tuning, and no placeholder calibration facts presented as real context.

Covers review items: P1-10, P1-11, P1-12, P2-20, P2-21.
</objective>

<context>
Read first:
- `.planning/phases/19-frontend-ui-ux-analysis-console-redesign/19-CONTEXT.md`
- `.planning/phases/19-frontend-ui-ux-analysis-console-redesign/19-RESEARCH.md`
- `.planning/phases/19-frontend-ui-ux-analysis-console-redesign/19-PATTERNS.md`
- `.planning/phases/19-frontend-ui-ux-analysis-console-redesign/19-01-PLAN.md`
- `tasks/frontend-ui-ux-review.md`
- `frontend/app/results/[id]/page.tsx`
- `frontend/components/ResultsCard.tsx`
- `frontend/components/DemoDetail/TraceCard.tsx`
- `frontend/components/DemoDetail/TraceComponentChart.tsx`
- `frontend/components/Analytics/SensitivityTuner.tsx`
- `frontend/lib/hooks/useDemoFetch.ts`
- `frontend/lib/hooks/useDemoDetail.ts`
- `frontend/lib/hooks/useTraceQuery.ts`
- `frontend/lib/hooks/useSensitivityTuner.ts`
- `frontend/lib/types.ts`
</context>

<tasks>
<task>
  <id>19-03-01</id>
  <title>Reframe results page around evidence hierarchy</title>
  <type>implementation</type>
  <files>
    - frontend/app/results/[id]/page.tsx
    - frontend/__tests__/components/ResultsCard.test.tsx
  </files>
  <action>
    Use `ConsolePage` and `ConsoleHeader` to create a results page that starts with demo metadata, processing state, research-signal notice, and primary summary. Organize content into an evidence hierarchy: overview/results, TRACE, feature vectors/sensitivity, and viewer. Tabs may be used if they improve navigation, but the page must still expose the primary result without hiding it behind a marketing-like section. Replace fragile back glyph with a Lucide icon if not already done.
  </action>
  <acceptance_criteria>
    - The conclusion and top contributing signals appear before raw controls.
    - Pending, timeout, error, not-found, and unknown states use console patterns and recovery actions.
    - The sensitivity tuner is labeled as what-if/validation, not a live model setting.
    - No route/API contract changes are required.
  </acceptance_criteria>
  <verify>
    cd frontend; npm test -- ResultsCard
  </verify>
</task>

<task>
  <id>19-03-02</id>
  <title>Redesign ResultsCard for concise evidence summary</title>
  <type>implementation</type>
  <files>
    - frontend/components/ResultsCard.tsx
    - frontend/__tests__/components/ResultsCard.test.tsx
  </files>
  <action>
    Convert `ResultsCard` into a compact evidence summary that uses `StatusBadge`, `DataValue`, and console panels. Replace spinner glyphs with Lucide `Loader2`. Show suspicion/verdict as "review signal" language, not proof. Surface top player rows and feature summaries in a scannable way, preserving existing `FeatureTable` where appropriate.
  </action>
  <acceptance_criteria>
    - Pending state uses accessible spinner/skeleton and pipeline-specific copy.
    - Done state labels suspicion as a research/review signal.
    - Player cards are compact and data-dense without nested-card clutter.
    - Download action remains available.
  </acceptance_criteria>
  <verify>
    cd frontend; npm test -- ResultsCard
  </verify>
</task>

<task>
  <id>19-03-03</id>
  <title>Reorganize TRACE card into score, components, context, calibration</title>
  <type>implementation</type>
  <files>
    - frontend/components/DemoDetail/TraceCard.tsx
    - frontend/components/DemoDetail/TraceComponentChart.tsx
    - frontend/components/DemoDetail/TraceChart.tsx
    - frontend/components/DemoDetail/CalibrationContextCard.tsx
    - frontend/__tests__/components/DemoDetail/TraceCard.test.tsx
  </files>
  <action>
    Rework `TraceCard` into clear sections: score summary, component breakdown, optional player context, and calibration metadata. Add brief text explanations for Base, Adjusted, Normalized, and Trust Multiplier. Do not render placeholder calibration assumptions as real data; hide or label unavailable context until backend data exists. Ensure charts have labels/exact values and are understandable without color alone.
  </action>
  <acceptance_criteria>
    - No TODO placeholder calibration values are presented as factual global averages.
    - Component bars/charts include visible labels and values.
    - Trust multiplier tooltip/info is keyboard accessible or has visible explanatory copy.
    - Error/retry behavior remains intact.
  </acceptance_criteria>
  <verify>
    cd frontend; npm test -- TraceCard
  </verify>
</task>

<task>
  <id>19-03-04</id>
  <title>Make sensitivity tuner neutral and more legible</title>
  <type>implementation</type>
  <files>
    - frontend/components/Analytics/SensitivityTuner.tsx
    - frontend/__tests__/components/Analytics/SensitivityTuner.test.tsx
  </files>
  <action>
    Update sensitivity tuner copy and controls. Replace "Safe" with "Below threshold" or similarly neutral language. Add current feature score markers/labels to each threshold control where feasible. Clarify the action sequence: reset, validate/save comparison, copy result. Ensure triggered/below-threshold states use text plus icon/shape/color.
  </action>
  <acceptance_criteria>
    - Tuner cannot be read as changing production/model behavior.
    - Trigger labels avoid innocence/proof language.
    - Copy/export output remains research-safe.
    - Existing hook behavior and backend validation mutation remain compatible.
  </acceptance_criteria>
  <verify>
    cd frontend; npm test -- SensitivityTuner
  </verify>
</task>
</tasks>

<threat_model>
T-19-05: Result/TRACE redesign could overstate model confidence or make scores appear like proof.
Mitigation: persistent research notice, neutral copy, "review signal" labels, and tests around copy.

T-19-06: Placeholder calibration data could mislead users.
Mitigation: hide or explicitly mark unavailable calibration context until real backend data exists.
</threat_model>

<verification>
- `cd frontend; npm test -- ResultsCard`
- `cd frontend; npm test -- TraceCard`
- `cd frontend; npm test -- SensitivityTuner`
- `cd frontend; npm run lint`
</verification>

<success_criteria>
- Results page presents a clear evidence hierarchy.
- TRACE details are understandable and do not display fake calibration context.
- Sensitivity tuning is clearly a what-if analysis aid.
- All high-impact scoring copy remains research-safe.
</success_criteria>
