---
phase: 19
plan: 02
type: implementation
wave: 2
depends_on: ["19-01-PLAN.md"]
files_modified:
  - frontend/app/dashboard/page.tsx
  - frontend/components/QuickUploadCard.tsx
  - frontend/components/SteamMatchHistoryCard.tsx
  - frontend/components/DemoHistoryTable.tsx
  - frontend/components/Analytics/FilterSidebar.tsx
  - frontend/components/Dashboard/IngestionActionsPanel.tsx
  - frontend/components/Dashboard/PipelineStatusPanel.tsx
  - frontend/components/Dashboard/ScopedDemoList.tsx
  - frontend/__tests__/components/Dashboard/DashboardConsole.test.tsx
  - frontend/__tests__/components/SteamMatchHistoryCard.test.tsx
autonomous: true
requirements_addressed: ["UI-01"]
---

# 19-02-PLAN: Dashboard Workflow Console

<objective>
Redesign the authenticated dashboard into a task-oriented analysis console: clear ingestion actions, match-history status, pipeline status summary, accessible filters, scoped demo review list, and history. Preserve existing hooks/API contracts and the secret-safety boundary from Phase 18.

Covers review items: P1-7, P1-8, P1-9, P2-19, P2-20, P2-21.
</objective>

<context>
Read first:
- `.planning/phases/19-frontend-ui-ux-analysis-console-redesign/19-CONTEXT.md`
- `.planning/phases/19-frontend-ui-ux-analysis-console-redesign/19-RESEARCH.md`
- `.planning/phases/19-frontend-ui-ux-analysis-console-redesign/19-PATTERNS.md`
- `.planning/phases/19-frontend-ui-ux-analysis-console-redesign/19-01-PLAN.md`
- `tasks/frontend-ui-ux-review.md`
- `frontend/app/dashboard/page.tsx`
- `frontend/components/QuickUploadCard.tsx`
- `frontend/components/SteamMatchHistoryCard.tsx`
- `frontend/components/DemoHistoryTable.tsx`
- `frontend/components/Analytics/FilterSidebar.tsx`
- `frontend/lib/hooks/useFilteredDemos.ts`
- `frontend/lib/hooks/useSteamMatchHistory.ts`
- `frontend/lib/hooks/useImportSharecode.ts`
- `frontend/lib/types.ts`
</context>

<tasks>
<task>
  <id>19-02-01</id>
  <title>Create dashboard-specific console panels</title>
  <type>implementation</type>
  <files>
    - frontend/components/Dashboard/IngestionActionsPanel.tsx
    - frontend/components/Dashboard/PipelineStatusPanel.tsx
    - frontend/components/Dashboard/ScopedDemoList.tsx
    - frontend/__tests__/components/Dashboard/DashboardConsole.test.tsx
  </files>
  <action>
    Create dashboard panels built from Wave 1 console primitives. `IngestionActionsPanel` groups upload, sharecode/import entry points if present, and match-history connect status. `PipelineStatusPanel` computes compact counts from available filtered/history data: pending, done, error, failed import/tracking attention where available, and last refresh/check labels. `ScopedDemoList` renders filtered demo rows/cards using `StatusBadge`, `DataValue`, relative time with exact timestamp title, map/outcome/status badges, TRACE/suspicion preview, empty state, loading skeletons, and load-more.
  </action>
  <acceptance_criteria>
    - Panels do not fetch data directly if an existing hook already owns it; pass props from dashboard where practical.
    - Empty, loading, error, pending, done, and failed states are visually distinct by text and shape, not color alone.
    - Demo rows remain clickable and keyboard focusable.
    - Tests cover status rendering, empty state, loading state, and row activation.
  </acceptance_criteria>
  <verify>
    cd frontend; npm test -- DashboardConsole
  </verify>
</task>

<task>
  <id>19-02-02</id>
  <title>Refactor dashboard page around console hierarchy</title>
  <type>implementation</type>
  <files>
    - frontend/app/dashboard/page.tsx
  </files>
  <action>
    Replace the current three-column widget layout with a `ConsolePage` layout. First viewport should show: user/session context, research-signal notice, ingestion actions, tracking state, and pipeline status. The primary work area should pair filters with scoped demos. History should appear below as secondary context. Preserve authentication redirect behavior, `useFilteredDemos`, `DemoHistoryTable`, `QuickUploadCard` upload success refresh behavior, and `SteamMatchHistoryCard` functionality.
  </action>
  <acceptance_criteria>
    - First viewport answers what is active, what needs attention, and what the current analysis scope is.
    - Upload, sharecode/import, and match-history tracking are grouped as ingestion/provenance workflows.
    - Existing dashboard filters and demo history remain usable.
    - Layout is usable at 320px, 768px, 1024px, and 1440px without horizontal scroll.
  </acceptance_criteria>
  <verify>
    cd frontend; npm test -- DashboardConsole
  </verify>
</task>

<task>
  <id>19-02-03</id>
  <title>Upgrade dashboard forms and tracking card copy</title>
  <type>implementation</type>
  <files>
    - frontend/components/QuickUploadCard.tsx
    - frontend/components/SteamMatchHistoryCard.tsx
    - frontend/__tests__/components/SteamMatchHistoryCard.test.tsx
  </files>
  <action>
    Migrate upload/tracking cards to console panel styling. Add inline client-side validation for plausible sharecode/link seed shape before connect while preserving backend validation as source of truth. Keep `steamidkey` as password input, clear it after successful connect, never echo it, and show privacy-safe helper text. Replace generic status copy with pipeline-specific language such as "Tracking caught up", "User action required", "Backoff active", or "Steam temporarily unavailable". Keep metadata copy framed as import/provenance only.
  </action>
  <acceptance_criteria>
    - Submit is disabled while mutation is pending and when required inputs are clearly empty/invalid.
    - Plaintext `steamidkey` is never rendered after submission.
    - Last error/status messages distinguish invalid seed/auth failed/rate limited/Steam unavailable.
    - Copy never implies match-history metadata affects suspicion or TRACE.
  </acceptance_criteria>
  <verify>
    cd frontend; npm test -- SteamMatchHistoryCard
  </verify>
</task>

<task>
  <id>19-02-04</id>
  <title>Normalize dashboard filters and history table styling</title>
  <type>implementation</type>
  <files>
    - frontend/components/Analytics/FilterSidebar.tsx
    - frontend/components/DemoHistoryTable.tsx
  </files>
  <action>
    Update filter controls and history table/card views to use console tokens, `StatusBadge`, `DataValue`, stable focus, and consistent loading/empty/error patterns. Replace text arrow sort glyphs with Lucide icons. Preserve existing filter persistence, callback signatures, sorting, pagination, and route navigation.
  </action>
  <acceptance_criteria>
    - Filter chips/buttons are keyboard visible and have disabled states while loading.
    - History desktop and mobile layouts use the same status/suspicion language as scoped demo list.
    - Sort indicators are icons, not fragile glyphs.
    - No proof language is introduced.
  </acceptance_criteria>
  <verify>
    cd frontend; npm test -- DemoHistoryTable
  </verify>
</task>
</tasks>

<threat_model>
T-19-03: Redesigning the dashboard could accidentally expose secret match-history credentials or retain them in rendered state.
Mitigation: preserve Phase 18 secret-clearing behavior, add tests for absent plaintext/ciphertext, and keep secrets out of URLs.

T-19-04: Dashboard copy could imply Steam tracking or import provenance is detection evidence.
Mitigation: use `ResearchSignalNotice`, provenance wording, and tests/assertions around no proof language.
</threat_model>

<verification>
- `cd frontend; npm test -- DashboardConsole`
- `cd frontend; npm test -- SteamMatchHistoryCard`
- `cd frontend; npm test -- DemoHistoryTable`
- `cd frontend; npm run lint`
</verification>

<success_criteria>
- Authenticated dashboard reads as one cohesive console.
- Users can see ingestion state, tracking state, pipeline state, current scope, and review-priority demos in the first workflow.
- Existing upload, filter, history, and match-history functionality remains intact.
- Dashboard responsive behavior passes 320px/768px/1024px/1440px inspection.
</success_criteria>
