---
phase: 19
plan: 01
type: implementation
wave: 1
depends_on: []
files_modified:
  - frontend/app/globals.css
  - frontend/app/layout.tsx
  - frontend/components/Navbar.tsx
  - frontend/components/Console/ConsolePage.tsx
  - frontend/components/Console/ConsoleHeader.tsx
  - frontend/components/Console/ConsolePanel.tsx
  - frontend/components/Console/ConsoleMetric.tsx
  - frontend/components/Console/StatusBadge.tsx
  - frontend/components/Console/ResearchSignalNotice.tsx
  - frontend/components/Console/DataValue.tsx
  - frontend/components/Console/index.ts
  - frontend/__tests__/components/Console/ConsolePrimitives.test.tsx
  - frontend/__tests__/components/Navbar.test.tsx
autonomous: true
requirements_addressed: ["UI-01"]
---

# 19-01-PLAN: Console Design System Foundation

<objective>
Create the reusable visual and interaction foundation for the Phase 19 analysis console: semantic design tokens, shared console layout primitives, research-signal notice, status badges, mono data-value styling, and first-pass navbar/glyph/focus polish.

Covers review items: P0-1, P0-2, P0-3, P1-4, P1-5, P1-6.
</objective>

<context>
Read first:
- `.planning/phases/19-frontend-ui-ux-analysis-console-redesign/19-CONTEXT.md`
- `.planning/phases/19-frontend-ui-ux-analysis-console-redesign/19-RESEARCH.md`
- `.planning/phases/19-frontend-ui-ux-analysis-console-redesign/19-PATTERNS.md`
- `tasks/frontend-ui-ux-review.md`
- `frontend/AGENTS.md`
- `frontend/node_modules/next/dist/docs/01-app/01-getting-started/03-layouts-and-pages.md`
- `frontend/node_modules/next/dist/docs/01-app/01-getting-started/05-server-and-client-components.md`
- `frontend/node_modules/next/dist/docs/01-app/01-getting-started/11-css.md`
- `frontend/node_modules/next/dist/docs/01-app/01-getting-started/13-fonts.md`
- `frontend/app/globals.css`
- `frontend/app/layout.tsx`
- `frontend/components/ui/button.tsx`
- `frontend/components/ui/card.tsx`
- `frontend/components/ui/badge.tsx`
- `frontend/components/Navbar.tsx`
</context>

<tasks>
<task>
  <id>19-01-01</id>
  <title>Add console semantic tokens and typography hooks</title>
  <type>implementation</type>
  <files>
    - frontend/app/globals.css
    - frontend/app/layout.tsx
  </files>
  <action>
    Extend the global theme with Phase 19 console tokens for surfaces, borders, severity, provenance, TRACE, and heatmap colors. Keep the existing shadcn/base-ui variables compatible, but add product-level variables such as `--surface-base`, `--surface-panel`, `--surface-raised`, `--border-subtle`, `--signal-clean`, `--signal-review`, `--signal-high`, `--trace-primary`, `--trace-accent`, `--heatmap-cold`, and `--heatmap-hot`. Map these into `@theme inline` as Tailwind colors. Preserve Geist or switch only if Next font setup remains stable; in either case, expose clear heading/body/mono variable usage.
  </action>
  <acceptance_criteria>
    - Existing shadcn primitives continue to render with `bg-card`, `text-card-foreground`, `bg-background`, and `text-foreground`.
    - New semantic tokens are available as Tailwind utility colors.
    - Dark mode is the primary polished console look; light mode remains readable.
    - No new network-loaded font CSS is introduced outside Next font handling.
  </acceptance_criteria>
  <verify>
    cd frontend; npm run lint
  </verify>
</task>

<task>
  <id>19-01-02</id>
  <title>Create console primitive components</title>
  <type>implementation</type>
  <files>
    - frontend/components/Console/ConsolePage.tsx
    - frontend/components/Console/ConsoleHeader.tsx
    - frontend/components/Console/ConsolePanel.tsx
    - frontend/components/Console/ConsoleMetric.tsx
    - frontend/components/Console/StatusBadge.tsx
    - frontend/components/Console/ResearchSignalNotice.tsx
    - frontend/components/Console/DataValue.tsx
    - frontend/components/Console/index.ts
    - frontend/__tests__/components/Console/ConsolePrimitives.test.tsx
  </files>
  <action>
    Add a small `Console` component module. `ConsolePage` should provide consistent page background, max-width, padding, and responsive spacing. `ConsoleHeader` should expose title, description, metadata, action, and notice slots. `ConsolePanel` should be the standard analysis panel. `ConsoleMetric` should render compact counts/scores. `StatusBadge` should cover demo statuses, import/tracking states, suspicion bands, TRACE availability, and neutral provenance. `ResearchSignalNotice` should render concise non-blocking copy. `DataValue` should render mono IDs/scores/ticks/sharecodes/calibration IDs with truncation support.
  </action>
  <acceptance_criteria>
    - Components are composable client-safe React components with typed props.
    - `StatusBadge` never uses color alone; it includes visible text and optional Lucide icon/shape.
    - `ResearchSignalNotice` copy says signals are for review/research and not proof.
    - Tests cover basic rendering, status variants, truncation, and notice copy.
  </acceptance_criteria>
  <verify>
    cd frontend; npm test -- ConsolePrimitives
  </verify>
</task>

<task>
  <id>19-01-03</id>
  <title>Polish navbar glyphs, focus states, and console navigation affordance</title>
  <type>implementation</type>
  <files>
    - frontend/components/Navbar.tsx
    - frontend/__tests__/components/Navbar.test.tsx
  </files>
  <action>
    Replace fragile text dropdown glyphs with Lucide icons, add accessible labels to the mobile menu toggle, and ensure all navbar interactive elements have visible `focus-visible` styling. Keep public landing anchors working while adding authenticated console navigation affordances for Dashboard, Analytics, Leaderboards, and History when a session exists. Avoid large visual shifts and preserve Steam login branding.
  </action>
  <acceptance_criteria>
    - No mojibake or fragile arrow glyphs are rendered in navbar.
    - Steam login button has a visible keyboard focus state.
    - User dropdown can be reached and understood by keyboard/screen reader users.
    - Public unauthenticated layout remains compatible with the landing page.
  </acceptance_criteria>
  <verify>
    cd frontend; npm test -- Navbar
  </verify>
</task>

<task>
  <id>19-01-04</id>
  <title>Add console primitive documentation through tests and exports</title>
  <type>implementation</type>
  <files>
    - frontend/components/Console/index.ts
    - frontend/__tests__/components/Console/ConsolePrimitives.test.tsx
  </files>
  <action>
    Make all console primitives available through a single index export and add tests that demonstrate intended usage patterns for a console page header, a research notice, and a metric row. Keep the module intentionally small; do not migrate pages in this plan except navbar polish.
  </action>
  <acceptance_criteria>
    - Downstream plans can import from `@/components/Console`.
    - Tests encode the expected non-proof wording and key CSS classes/roles.
    - No authenticated page is substantially redesigned before Wave 2.
  </acceptance_criteria>
  <verify>
    cd frontend; npm test -- ConsolePrimitives
  </verify>
</task>
</tasks>

<threat_model>
T-19-01: Design token changes could break contrast or make status semantics ambiguous.
Mitigation: preserve base shadcn tokens, add semantic tokens incrementally, test rendered status text, and keep color paired with labels/icons.

T-19-02: Research-signal wording could be omitted from high-impact pages if it remains ad hoc.
Mitigation: create a reusable `ResearchSignalNotice` in the foundation wave and require later plans to use it.
</threat_model>

<verification>
- `cd frontend; npm test -- ConsolePrimitives`
- `cd frontend; npm test -- Navbar`
- `cd frontend; npm run lint`
</verification>

<success_criteria>
- Console semantic tokens exist and do not break current app rendering.
- Shared console primitives are implemented and tested.
- Navbar no longer renders fragile glyphs and has visible focus states.
- Later waves can migrate pages without duplicating layout, badge, notice, or data-value patterns.
</success_criteria>
